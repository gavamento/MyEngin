# review-1 — M76 Deep-Modal (round 1)

- 日付: 2026-09-16
- 対象コミット範囲: `99803eb..84ce5e9` (M76a 〜 M76h + 台帳コミット、9 サブ)
- 仕様: `plans/m76-deepmodal/spec.md` (確定 2026-09-16) / `sub-01.md`〜`sub-09.md` / `harness.md`
- 実行環境: Windows 11 / VS2022 (MSBuild 18) / x64。AVX2 対応機、`--modal-bake` の既定スレッド数で計測

---

REVIEW: FAIL
round: 1
軸 (1-5):
  製品の深度: 3 — 機構は本物で、焼いた特徴マップは形状・接触位置・力・材質・サイズのすべてに依存していることを自分で確認した (382 枚の `.msfm` を解析: mask-on 率 0〜88%、cell ごとの mask パターンが 1〜347 種)。しかし**出荷状態の絶対音量が実用域に無い**: `ampScale` が較正前のプレースホルダ 1.0 のままで、`--modal-demo` が鳴るのは箱が 0.7〜7.9 トンあるからにすぎない (J = 161〜102164 N·s)。実測した peak/J は線形 (Metal 1.15e-6/N·s、Wood 2.41e-6/N·s) なので、int16 の 1 LSB (3.05e-5) に届くのが **J ≈ 11〜27 N·s**、「聞こえる」水準 (-40 dBFS) には **J ≈ 3600〜8700 N·s** が要る。1 kg の小物を 0.5 m 落とす (J ≈ 3 N·s) と PCM は全サンプル 0 になる。加えて焼いた 382 枚のうち **35 枚 (9.2%) は全 cell・全帯域で mask が落ちており、どんな力積でも無音**。この制約は `plans/.../spec.md §7` にはあるが、**エンジン側の文書 (engine_spec §10.7 / ADR-020 / README) には 1 行も無い**
  機能性: 4 — spec §5 の受け入れ条件のうち自分で回せるものは**ほぼ全部緑**だった (下の「検証した手段」に 1 件ずつ)。`replay_verify` 13 ジョブ全緑、`.msfm` のスレッド数非依存 (T=1/3/4/5 でバイト一致)、`--modal-demo` の 2 run が `[modal] t=` 20 行バイト一致、`--modal-bake` 453.65 ms/mesh (条件 22 の 0.6 s 以内) → 2 回目 0.54 ms。減点は指摘 2 (Inspector プレビューが既定値で無反応) と指摘 5 (`--modal-bake` が `--modal-backend` を黙って無視)
  ビジュアルデザイン: 4 — Inspector の ModalSound 節を実機で開いて確認した (`scratchpad/f1c.png`, `insp5.png`): `State: Ready (cpu)` / `Cells: 3375` / `Impulse (N*s)` スライダ / `+X -X +Y -Y +Z -Z` / `Export WAV` が既存 Inspector の体裁どおりに並び、読みやすさ・一貫性に問題は無い。減点は「押しても何も起きず、なぜ起きないかも出ない」状態 (指摘 2) が**見た目からは一切分からない**こと。PhysMat の 4 行は GUI 自動操作が前面化で不安定になったためコード読み + `LocalizationSelfTest` 緑までの確認 (実機目視は未実施、正直に記す)
  コード品質: 4 — 「規則は 1 本」は本当に守られている (`BuildModes` / `ModalSynthRender` / `MakeModalShotPlay` をランタイム・Inspector プレビュー・`--modal-face-probe` の 3 経路が共有、`RestingImpulse` は `DrainImpacts` と共有)。include の向きも実測で一方向 (`src/Engine/Engine/Modal/` から Audio/Acoustic/World への include は 0 件)。日本語コメントは「なぜ」と「踏んだ罠」を書いている (`CpuModalBackend.cpp:37-50` の chunk 量子化の説明が白眉)。仮実装の残骸も無い。減点は指摘 3・4・6 (`.msfm` の書き込み増幅、Mel のドリフト検査が同語反復、ModalSound が 1 個も無いシーンでも毎 tick テーブルを作る)

指摘:
  1. [major] 宛先: planner — **`ampScale` が未較正のまま出荷されており、現実的な質量・落差の物体では「衝突音が鳴る」という目的 (spec §1) が成立しない。しかもこの制約がエンジン側の文書に無い** — 根拠:
     (a) `assets/deepmodal/deepmodal.dmnet` のヘッダを直接読むと `ampScale = 1.0`、`logAmpMin=-27.625 / logAmpMax=-5.957`。
     (b) `Runtime.exe --modal-demo --modal-sync-bake --modal-audio-log 300 --synth-input --screenshot ... --frames 300` の 20 発は J = 161〜102164 N·s。デモの箱は 1 m³ の中実木材/金属 (= 0.7 t / 7.85 t) なのでこの桁になる。
     (c) `--modal-wav-dump` で書いた実 PCM を測ると peak は J に対して線形: MetalBox `peak/J = 1.150e-6` (J=102164→0.11752, J=860→0.00098)、WoodBox `2.41e-6`、GlassBox `2.79e-6`。⇒ int16 の 1 LSB (3.05e-5) に届く下限が **J ≈ 11〜27 N·s**、-40 dBFS には **J ≈ 3600〜8700 N·s**。
     (d) `--modal-face-probe` が 15 N·s で書いた 3 枚 (`probe_14e9a6924b79f079_{px,nx,pz}.wav`) は **peak 0.00006 = -84.3 dBFS = 振幅 2 LSB**。ADR-020「決定 5」はこの 3 枚を「面で音が変わる」の肯定的証拠として挙げているが、**その絶対レベルが 2 LSB であることは書かれていない**。
     (e) `grep ampScale docs/adr/ADR-020-deep-modal.md README.md engine_spec.md` → engine_spec の `.dmnet` 書式表に語が 1 回出るだけで、**「較正前のプレースホルダ 1.0」「軽い接触は無音」はどの engine 文書にも無い**。記録は `plans/m76-deepmodal/spec.md §7` (ハーネスの計画ファイル) だけ。
     (f) 焼いた 382 枚のうち **35 枚 (9.2%) は mask が全 cell・全チャンネルで off** = 力積に依らず常に BelowMin (`.msfm` を直接解析)。この実測値もどこにも記録が無い。
     — 期待: 次のどちらかを選び、spec の変更履歴に残すこと。(A) **いま較正する** — `export.py --amp-scale` は既にあり、`.dmnet` ヘッダの float 1 個なので再学習は不要。ただし spec §2 #12 が予見した「重い衝突がソフトクリップに張り付く」が同時に顕在化するので、上限圧縮を入れるかの判断とセットで行う。(B) **較正しないと決めるなら §7 のリスク欄から §3「やらない」へ格上げし**、engine_spec §10.7 / ADR-020 / README に「現行の `.dmnet` は `ampScale` 未較正で、実用的な音量を得るには `ModalSound.gain` を手で上げる必要がある (目安 J≈10 N·s で gain ≈ 10³)」を明記して、較正を独立した後続マイルストーンとして名前を付けること。**今の状態は「動くが誰も使えない」で、それが engine 文書からは読み取れない**のが問題。

  2. [major] 宛先: coder — **Inspector の面打ちプレビューが既定値 (Impulse = 4.0 N·s) では 6 面すべて無反応で、何が起きなかったのかも一切出ない** — 根拠: 実機で `Editor.exe --modal-demo` を起動し WoodBox を選択、`Impulse (N*s)` が既定の **4.00** の状態で `+X -X +Y -Y +Z -Z` の 6 個を順に押した (`scratchpad/f2c.png`)。**`Export WAV` は disabled のまま** = `modalPreview_.valid` が false = 6 面とも `Played` にならなかった。スライダを 19.73 N·s へ上げて `+X` / `-X` を押すと `Export WAV` が有効化される (`scratchpad/f3c.png`) ので、機構自体は生きている。コードは `InspectorWindow.cpp` の `FireModalPreviewFace` が `if (result != ModalShotResult::Played) { return; }` で**ログもトーストも状態表示も出さずに戻る**、既定値は `InspectorWindow.h` の `ModalPreviewState::impulse = 4.0f`。同じ round で coder 自身が**ヘッドレス側の双子の定数**を `AudioSourceSystem.cpp` の `kProbeImpulse` 4.0f → **15.0f** へ上げている (「4.0 では 6 面すべて BelowMin になった」と sub-08 round 2 に明記) のに、**Inspector 側の 4.0 は据え置かれた**。spec §7 も「4 N·s では 6 面すべて BelowMin」を記録済み。⇒ 受け入れ条件 18 の手動確認「6 面で音が変わる」は、出荷状態の UI を触っただけでは**再現できない**。 — 期待: (i) `BelowMin` (および `NotReady` / `PoolFull`) を UI に返すこと — `State:` 行の隣に直近プレビューの結果を出す / `MYE_LOG_INFO` を 1 本出す / ボタンを押した瞬間に理由を出す、のいずれか。「押しても無音」と「押しても何も起きていない」をユーザーが区別できないのが本質。(ii) 既定 Impulse を `kProbeImpulse` と同じ根拠で揃える (定数が 2 箇所にあるなら 1 本化する)。なお「上げても -84 dBFS で耳には届かない」は指摘 1 の管轄で、こちらの範囲ではない。

  3. [minor] 宛先: coder — **`.msfm` をサブメッシュ 1 枚ごとに表ごと書き直しており、初回焼きのディスク書き込みが O(n²) になる** — 根拠: `src/Engine/Engine/Modal/ModalSoundLibrary.cpp` の `UpdateTableEntry` が毎回 `SaveTable(srcPath, table)` を呼び、`SerializeModalTable` が**表全体**を作り直して `CookedCache::Write` する。実測: `--modal-bake` の生成物は 19 ファイル / 382 枚 / 最終合計 55 MB だが、最大のもの (`2ec7bacbd78c3594.msfm`) は **144 枚で最終 15.1 MB** なので累積書き込みは約 1.09 GB、全体で **約 2.2 GB** (最終サイズの 40 倍)。spec §4.2 の文言は「遅延で書き足す」。 — 期待: モデル単位でバッチの終わりに 1 回だけ flush する (dirty フラグ + `--modal-bake` / ライブラリ破棄時の明示 flush)。挙動は変わらず、初回起動の裏作業の I/O だけが減る。

  4. [minor] 宛先: coder — **Mel 帯域中心の「ドリフト検知」テストが同語反復で、本来守るべき C++ ⇄ Python の食い違いを検出できない** — 根拠: `src/Engine/Engine/Audio/ModalSynthSelfTest.cpp:145-167` は `MelBandCenters()` (`ModalTypes.h:87-97`) と**同じ式を同じファイルに書き写して**比較している。spec §4.1 が要求しているのは「C++ の式は selftest が**ヘッダ値**と 1e-2 Hz で照合 (ドリフト検知)」で、照合相手は Python (`compact.py`) が焼いた `.dmnet` の `bandCenterHz[32]`。`ModalSelfTest.cpp` は `fixture.dmnet` を既にロードしているのに `bandCenterHz` を 1 度も見ていない (`grep bandCenterHz src/Engine/Engine/Modal/ModalSelfTest.cpp` → 0 件)。**現物にドリフトは無い** (私が `assets/deepmodal/deepmodal.dmnet` のヘッダを読み出して C++ の式と突き合わせた結果 max |Δ| = 3.7e-4 Hz) が、番人が居ない。 — 期待: `ModalSelfTest` のフィクスチャ節で `net.header.bandCenterHz[i]` と `MelBandCenters()` を 1e-2 Hz で照合する 1 本を足す。

  5. [minor] 宛先: coder — **`--modal-bake` が `--modal-backend` を黙って無視する (README はこの組み合わせを有効なものとして書いている)** — 根拠: `src/Editor/ModalTools.cpp:373` が `lib.SetBackendByName("cpu");` を**ハードコード**しており、`RunModalBakeCli` は `config.modalBackendName` を受け取らない (`src/Editor/EditorMain.cpp:434` は `projectDir` しか渡さない)。実行: `cmd /c "bin\x64\Release\Editor.exe --modal-backend d3d11cs --modal-bake"` → exit 0、ログは `[modal] loaded .dmnet: ... (backend=cpu)` のみで **WARN が 1 行も出ない**。一方 `README.md` は `Editor.exe --modal-bake [--modal-backend cpu|d3d11cs]` と書き、engine_spec §10.7 の CLI 表も `--modal-backend` を「未実装の d3d11cs は WARN して cpu へ落ちる」と説明している。受け入れ条件 13 の「綴り違いは exit 1」は CLI パーサ側で満たされている (`--modal-backend bogus --modal-bake` → exit 1 を確認)。今日は cpu しか実装が無いので実害は無いが、**`D3d11ModalBackend` が入った日に「GPU で焼いたつもりが cpu だった」が無警告で起きる** — WARN を足した理由そのものの事故。 — 期待: `RunModalBakeCli` にバックエンド名を渡して `SetBackendByName` を通すか、`--modal-bake` と併用されたら明示的に「このコマンドは cpu 固定」と WARN すること (README もどちらかに合わせる)。

  6. [minor] 宛先: coder — **`ModalSound` を 1 個も持たないシーンでも、接触がある tick は毎回コライダ索引表を作って sort する** — 根拠: `src/Engine/Engine/Audio/ModalAudio.cpp` の `CollectModalImpacts` は `if (contacts.empty()) return;` の後、`world.ForEachArchetype` で全コライダを列挙して `std::sort` してから接触ループへ入る。`ModalSoundComponent` の有無を見るのはその後の `emitSide` の中。同じ表は同じ tick に `AcousticField::DrainImpacts` も作っているので、M76 以降は**既存の全プロジェクトが毎 tick 2 回**払う。`replay_verify` は 313 s で通っており実害は測れていないが、opt-in 機能の費用が opt-out できていない。 — 期待: 先頭で「World に `ModalSoundComponent` のアーキタイプが 1 つも無い」を確認して即 return する (`ForEachArchetype` の 1 回で判定できる)。

  7. [minor] 宛先: planner — **`--modal-demo` が spec §1 の 6 目的のうち「形状で音が変わる」だけを見せられない** — 根拠: `src/Engine/Engine/DemoContent.cpp` の `BuildModalShowcaseScene` は WoodBox / MetalBox / GlassBox の 3 個とも `res.meshes.Cube()` = **同一メッシュ** (実行ログでも 3 発とも `mesh=14e9a6924b79f079` で同じ)。材質・サイズ・力積の差は見えるが、形状差は見えない。形状依存そのものは焼いた `.msfm` を私が直接解析して確認できた (382 枚で mask-on 率 0.00%〜88.14%、cell ごとの mask パターン数 1〜347、`validCells` 6〜3375) ので**機構は正しい**。 — 期待: ショーケースに `builtin://sphere` / `cylinder` / `capsule` のいずれかを 1 個足す (生成順の末尾に足せば既存の replay/golden への影響は無い。spec §3 は golden/replay ペアを増やさない方針なので、demo シーンの中身だけで済む)。あるいは「形状差はデモでは見せない」と spec §3 に明記する。

  8. [minor] 宛先: planner — **spec §4.2 が要求した `.gitattributes` の `*.msfm binary` が、どの sub-NN にも割り当てられず入っていない** — 根拠: `spec.md §4.2` は「`.gitattributes` に `*.dmnet binary` / `*.mvox binary` / **`*.msfm binary`** / `tests/deepmodal/*.bin binary` を足す」と書いているが、`sub-02.md:29` は `*.mvox` のみ、`sub-04.md:25` は `*.dmnet` と `tests/deepmodal/*.bin` のみを指示しており、`grep msfm .gitattributes` → 0 件。`.msfm` は `cache/` (gitignore 配下) にしか生まれないので**実害は無い**。coder の「仕様との差分」が空だったことを黙った差分 (blocker) とは扱わない — これは仕様→サブ分割の落ち穂であって実装側の逸脱ではないため。 — 期待: 1 行足すか、spec §4.2 から消して「`.msfm` は cache 専用なので版管理の対象外」と書き直すこと。

  9. [minor] 宛先: coder — **ADR-020「決定 5」の材質値の記述が実物と食い違う** — 根拠: `docs/adr/ADR-020-deep-modal.md` の決定 5 は「金属は α=5・β=3e-8 と両方小さく長く鳴る」と書いているが、`--modal-demo` の MetalBox が使うのは `assets/physmats/metal.physmat.json` で **α=6.0 / β=1.0e-7**。α=5 / β=3e-8 は `assets/physmats/steel.physmat.json` の値。減衰時間の実測 (MetalBox 最長 len=1.367 s) は metal の値から導かれるので、**結論は正しく根拠の数字だけが間違っている**。 — 期待: ADR の数値を metal のものへ直す。

  10. [minor] 宛先: planner — **spec §4.4 の「初回の裏作業は合計 100 s 前後」の見積もりが、モデル数とサブメッシュ数を取り違えている** — 根拠: spec §4.4 は「エンジンの `assets` は 19 モデルなので初回の裏作業は合計 100 s 前後」としているが、実際に焼かれるのは**サブメッシュ単位**で `models=19 bakes=382`。実測は 1 回目 `bakeMsAvg=453.65 ms` / wall **183.6 s**。数字としては同じ桁で、この見積もりが支えていた結論 (「ネットを縮めない」) は変わらない。 — 期待: §4.4 の文言を「19 モデル = 382 サブメッシュ、実測 183 s」へ更新する (次に速度予算を議論するときの一次データになるため)。

検証した手段:
- **ビルド**: `MSBuild MyEngine.sln /p:Configuration=Debug|Release /p:Platform=x64 /p:MyeWarnAsError=true` → 両方 exit 0。C++ 警告 0 (出たのは既存の `LNK4204` (imgui の pdb) のみ)。
- **受け入れ条件 1〜4, 11〜15, 18 の自動部分**: `cmd /c "bin\x64\Debug\Editor.exe --selftest"` → **exit 0、FAIL 0 件** (4814 行。`ModalSynth self test` / `Modal self test` / `ModalAudio self test` / `PhysMat` / `ReloadHub (kind=14 rank=6)` / `EngineCli` の modal フラグ 8 本を含む)。
- **受け入れ条件 5**: `Editor.exe --modal-voxelize --list tests\deepmodal\list_builtin.txt --out DIR` → **exit 0 / `.mvox` 6 枚、各 32840 B** (= 72 B ヘッダ + 32768 B、spec §4.2 と一致)。`--list nope_missing.txt` → **exit 1** + `[modal-voxelize] ERROR: cannot open list:`。
- **受け入れ条件 6**: `cd tools\deepmodal && python -m pytest -q` → **40 passed**。
- **受け入れ条件 7 / 21**: `tools/deepmodal/data/stage0` に npz 38 本、`data/calibration.json` に capsule (16200 DOF) / sphere (45864 DOF) の直接法 ⇄ LOBPCG 突き合わせ (同一モード予算 34、`residual_max` 直接 2.0e-8 / LOBPCG 4.5e-4、周波数 `rel_err_max` 2.1e-7、`missed_count` 0、帯域集計 `mean_rel_diff` 0.062)。残差しきい値の適用は `dataset.py:133-137` (`keep = finite & (residuals <= modal.RESIDUAL_DROP)`) で `method` による分岐が無いことを確認。
- **受け入れ条件 8**: `pwsh -File tools\check_rules.ps1` → **0 error(s), 0 warning(s)**。C++ ⇄ Python の 5 組 (`kModalVoxelN/kModalMapN/kModalBands/kModalChannels/kDmNetMaxParamCount`) が `$constGroups` にあり、**片側が見つからなければ `$errors++` する実装** (`check_rules.ps1:565-575`) なので「正規表現が空振りして静かに緑」にはならないことをコードで確認 (意図的な破壊テストはソース改変になるので行っていない)。
- **受け入れ条件 10**: `tests/deepmodal/` に `fixture.dmnet` (71304 B) / `fixture_in.mvox` (32840 B) / `fixture_out.bin` (49408 B) / `list_builtin.txt` がコミット済み。
- **受け入れ条件 11 / 22**: `cmd /c "bin\x64\Release\Editor.exe --modal-bake"` 1 回目 → `models=19 bakes=382 **bakeMsAvg=453.65**` (0.6 s/mesh の合格線内、目標 0.3 s は未達)、wall 183.6 s。2 回目 → `bakeMsAvg=**0.54 ms**` (`.msfm` 命中)。参考: `MYE_MODAL_THREADS=1` で 1466.42 ms = AVX2 単スレッドの値。
- **受け入れ条件 23**: `MYE_MODAL_THREADS=1 / 3 / 4 / 5` で `cache\cooked` を消してから焼き直し、19 枚の `.msfm` を SHA-256 で比較 → **T=1 対 T=3 / T=4 / T=5 とも差分 0 枚**。SIMD 幅で割り切れない 3 / 5 を含む。
- **受け入れ条件 16**: `cmd /c "bin\x64\Release\Runtime.exe --modal-demo --modal-sync-bake --modal-audio-log 300 --synth-input --screenshot ... --frames 300"` を 2 回 → `[modal] t=` **20 行が完全一致 (Compare-Object の差分 0)**、`summary impacts=20 played=20 notReady=0 cooldown=0 belowMin=0 dropped=0 poolFull=0 playFailed=0`。`--no-audio` を足すと `[modal] t=` **0 行 / summary 0 行** (出るのは起動時の `.dmnet` ロード行 1 本だけ)。
- **受け入れ条件 17 / 決定論契約**: `MYE_REPLAY_JOBS=3 tools\replay_verify.bat` → **`[parallel] all 13 jobs passed in 313.0s`** + `[PASS] replay consistency (Debug/Release, 8 scenes ...) + snapshot round-trip + time travel + rule check`、FAIL/MISMATCH 行なし。sim 側の差分は `AcousticField.cpp` の `RestingImpulse` 抽出だけで、`git diff` を読む限り式も評価順もラムダも元のまま (`ModalAudioSelfTest.cpp:179` が抽出前の式とのビット一致も固定している)。`TickRunner.cpp:646-656` の `CollectModalImpacts` は `!ts.resim` ブロック内かつ `audioSystem.IsReady() && !IsSuspended()` の門の内側。`tests/golden/` は `git diff --stat 99803eb..HEAD -- tests/golden` が空 = **1 枚も触っていない** (`shot_verify.bat` 自体は再実行していない。司会の指示どおり既知の 2 枚は M76 の欠陥として数えていない)。
- **受け入れ条件 19**: `assets/deepmodal/deepmodal.dmnet` のバイト列を自分で解釈 → `magic=0x544e4d44 ver=1 ops=33 bufs=22 / inN=32 outN=16 bands=32 ch=192 / fMin=100 fMax=10000 / logAmp=-27.625..-5.957 / **ampScale=1.0** / maskTh=0.5 / refYoung=7.0e10 refDensity=2700 refPoisson=0.33 **refSizeL=0.6** refAlpha=6 refBeta=1e-7 / paramCount=1,682,448`、ファイル 3,368,464 B (3.21 MiB ≤ 4 MB)。`bandCenterHz[32]` を C++ の `MelBandCenters()` と突き合わせて **max |Δ| = 3.7e-4 Hz**。本学習の pooled R² = 0.6277 / mask acc 99.82% は sub-08 の学習曲線 10 点として記録済み (R² > 0 = 定数モデルより良い、という条件 19 の門を満たす)。README に ModelNet10 の手順・4.5 h の見積もり・実行禁止の門・再開方法あり。
- **受け入れ条件 3 の資産側**: `assets/physmats/*.physmat.json` 11 本すべてに `youngsModulus` / `poissonRatio` / `rayleighAlpha` / `rayleighBeta` が入っており、値は spec §4.2 の初期値と一致。
- **深度 (受け入れ条件の外側)**:
  - 焼いた `.msfm` 19 ファイル 382 枚を自作パーサで直接読み、fp16 特徴を展開して集計 → mask-on 率 0.00%〜88.14% (中央値 54.39%)、cell ごとの mask パターン数 1〜347 (中央値 50)、有効 cell 6〜3375。**35 枚 (9.2%) が全 cell 全チャンネル mask off = 常時無音**、52 枚が cell 間で mask が一切変わらない (= 接触位置に依存しない)。
  - `--modal-wav-dump` + `--modal-face-probe` で書いた 23 枚の WAV を Python (`wave` + FFT) で実測 → 材質差 (Wood 支配 1480 Hz / len 0.050-0.066 s、Metal 1121 Hz / 1.367 s、Glass 2336 Hz / 0.686 s)、力積差 (peak が J に対して単調・線形、120 倍のレンジ)、面差 (px 1980 / nx 2000 / pz 1800 Hz) を確認。ただし面プローブ 3 枚はいずれも **peak 0.00006 = -84.3 dBFS** (指摘 1)。
  - 実機の Inspector で 6 面ボタンを既定値・最大値の両方で押して挙動を確認 (指摘 2)。
  - `--modal-backend d3d11cs` / `bogus` / `--modal-bake` の組み合わせ (指摘 5)。
- **コード**: `git diff 99803eb..HEAD` のうち C++ 全域 (Modal/ 10 ファイル、Audio/Modal* 6 ファイル、TickRunner / AcousticField / AcousticAudio / AudioSourceSystem / EngineLoop / Components / InspectorWindow / ModalTools / PhysMatLibrary / AssetKeyResolver / ConvexColliderLibrary / ReloadHub / check_rules.ps1 / .gitattributes / .gitignore) と、文書 4 点 (engine_spec §10.7、ADR-020、README、CLAUDE.md)、Python は `layout.py` / `modal.py` / `dataset.py` の該当箇所。`grep -rn "#include" src/Engine/Engine/Modal/*` で Audio/Acoustic/World への依存 0 件を確認。`TODO/FIXME/HACK` の残骸 0 件。
- **見た画像**: `scratchpad/ed2.png`(エディタ全景) / `ed4.png` / `insp5.png` (ModalSound 節) / `f1c.png` (既定 Impulse=4.00) / `f2c.png` (6 面クリック後も Export WAV disabled) / `f3c.png` (19.73 N·s で Export WAV 有効)。

前回指摘の消込: (round 1 のため該当なし)

---

## 参考: 私が「問題なし」と確認した主な点 (後続 round で蒸し返さないために)

- 決定論契約は守られている。`ModalSound` は `kComponentNoHash`、push は `!ts.resim` + audio 門の内側、消費はフレーム側、乱数不使用、`unordered_*` からバイト列を作っていない (`.msfm` の表は key 昇順 vector、`UpdateTableEntry` が `lower_bound` で挿入順を保つ)。`replay_verify` 13 ジョブ全緑がその証人。
- `.msfm` はスレッド数に依らずバイト一致する。`ParallelSpan` の `align = kGemmAvx2Width(8)` によるチャンク量子化 (`CpuModalBackend.cpp:52-84`) が正しく効いており、AVX2 の 8 列ブロックとスカラー端数の境界がスレッド数から独立していることを T=1/3/4/5 の実測で確認した。round 1 の見逃し (2 冪だけでは検出できない) の分析もコメントに残っている。
- 「規則は 1 本」は本当に 1 本。`MakeModalShotPlay` をランタイム (`AudioSourceSystem::Update`)・Inspector (`FireModalPreviewFace`)・`--modal-face-probe` の 3 経路が共有し、`ResolveModalMesh` を衝突・口封じ・プレビューが共有し、`acoustic::RestingImpulse` を `DrainImpacts` と `CollectModalImpacts` が共有している。
- `RestingImpulse` の抽出はビット中立。`ConvexCookSourcePath` → `assetkey::SourcePathForSubAssetKey` の委譲も判定を変えていない。
- `--dmnet` が無い環境では `LoadModel` が false を返して `NoModel` のまま起動が続き、既存の音経路に影響しない (`EngineLoop.cpp:271-278` のコメントどおり)。
- 文書は質・量とも十分 (engine_spec §10.7 は「サイズ漏れ」バグの経緯まで書いている)。唯一の欠落が指摘 1(e)。
