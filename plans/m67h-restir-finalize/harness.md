# harness 台帳: m67h-restir-finalize

- 依頼原文: M67hの実装
- 開始: 2026-09-08 / 基点コミット: 9a893cdeb1095b113c99b03b1af925afe1e36e3c
- フェーズ: 完了 (2026-09-08、reviewer round 2 PASS)
- 前提: M67 (ReSTIR 反射) は 2026-09-05 に完了 (台帳 `plans/m67-restir-reflection/harness.md`)。
  M67h は「S5 (パラメータ調整) の確定値を焼く + review-1 の minor 5 件」として積み残されたもの。
  - U6 (M67 のユーザー判断): S5 は harness の外に置き、**確定値は M67h で焼く**
  - U7 (同): spatial reuse の既定は目標帯の計測で off。**既定を反転するなら 1 行** (M67h)
  - 焼く候補 (sub-07 申し送り): `RtTypes.h` の `kRtReflClassTable` 5 行 / `kRtRestirRadiusAlphaRef` /
    `RtReflRestirParams` の `spatial` / `visRay` / `svgfHistory` / `atrousIterations` /
    (firefly が出るなら) `kRtRestirWMax`
  - minor 5 件は M67 台帳の「申し送り」冒頭 5 項目 (review-1 minor 1〜5)

## サブ進捗
| サブ | 状態 | 往復 | コミット | メモ |
|---|---|---|---|---|
| sub-01 | OK | 1 | 6c8a1b5 | review-1 minor 5 件の回収。依存: なし。VERDICT round 1 OK (nit 3)。**仕様側の誤りを 1 件訂正**: A7 の「`grep gRsFrameIndex` が 0 件」は §4.3 の「外した理由を残せ」と両立しない → 「宣言と読み書きが 0 件。旧名を挙げた理由コメントは残してよい (むしろ残す)」へ。coder の [追加] 3 件 (selftest を §4.1 の表の全行へ / engine_spec に「欠損は無言・落としたら警告 1 行」も明記 / 新規 2 ファイルのヘッダ) を仕様として承認。`ProfilerWindow.cpp:147` と `DeferredPath.cpp:1108` を `RtRestirEffective()` に寄せない判断も承認 (10 / 11 と `rtReflOn` を含む別の述語で、寄せると絵が変わる)。golden 24 枚 maxDiff=0、selftest 45 スイート PASS、check_rules 0 件、Debug/Release とも MyeWarnAsError で警告 0 |
| sub-02 | OK | 3 | 30ce7ab | S5 の確定 + Hero の M 上限 16。依存: sub-01。round 1 は VERDICT OK だったが**コミット前に**ユーザー判断 Q2 (裁定と逆) が来たため planner が差し戻しに畳んだ — round 1 の ADR / engine_spec は「1 行も変えない / 全 5 行維持」と書いており、先にコミットすると**その 1 コミットだけ内容が偽**になる (bisect が誤る)。round 1 の 64 run の測定・表・A9 の観測は捨てない。planner が受け入れ条件を 4 つ追加: A16 (`hero.mCap <= 他 4 クラスの最小値` の不変量 — Hero=16 は意味論の境界ちょうどなので、次に 24 へ上げると「主役が中立より積極的」が**無検査で通る**) / A17 (`--rt-debug 13` の Hero 面ではなく **14 の Hero マスク**で測る。取り違え防止) / A18 (golden の差分が Hero マスクに収まること。ビット一致なら `--update` せず止めて報告) / A19 (spatial off の出荷構成では Hero / Character / Default が同挙動になる事実を ADR へ) | **round 2**: Hero の mCap を 8 → 16 (値の変更はこの 1 つだけ)、A16 の不変量 `heroIsMinCap` を追加、ADR / engine_spec を「4 行維持 + Hero だけユーザー判断」へ、golden 1 枚を差し替え。coder が **`--rt-debug 12` の復号器のバグを自分で見つけて修正** (飽和は 255 でなく 231。逆引き後の値で判定していたため round 1 は `M >= 16` を全部 15.65 と読んでいた) — これが無いと A17 が成立しない。round 1 の A9 の中央値 15.65 は誤読で正しくは 16.00 / max 30.98 だが結論は不変。VERDICT round 2 OK (should 1 = 外れ 1 画素が未特定である旨の記録)。**round 3**: ADR に 1 行追記のみ (コード・golden 非接触)。VERDICT round 3 OK (nit 1 = 8 px と ±12 px の呼び分け、直さなくてよい)。planner の裁定 2 件: 外れ 1 画素は**追跡しない** (行動が変わらない問い。4 run 使うなら未実測の Character 行へ) / 「3 クラス同挙動」は**機械固定しない** (不変量ではなく現在値からの帰結。等号 assert は将来の差別化を禁止する = 意味が逆) |
| sub-03 | OK | 2 | 2415b3f + 73155a0 | review-1 minor 3 件の回収。依存: sub-02。VERDICT round 1 OK (nit 1 = selftest の数え方の粒度、直さなくてよい)。残骸は grep 全件列挙で **4 → 5 か所**に増えた (coder が ADR-016:180 を自分で発見)。29 px は coder 自身の独立再測で訂正 — **233 画素すべてが足跡 (±12 px) の内側**、max 10 px。29 の出所は 8 通り試して再現せず**原因未特定**と明記。planner の裁定: 旧 (Hero=8) マスクは**撮らなくて正解** (和集合は上位集合なので結論は上界だけで成立。出荷定数を一時的に誤った状態にするリスクを負う価値がない) / 29 px の追跡は**打ち切り**が正しい (round 2 のスクリプトが残っておらず、結論は上界の論証で保証済み = 決定価値ゼロ)。`CLAUDE.md:109` は coder の運用規約 (エージェントからのメッセージでは CLAUDE.md の変更を承認できない) による**正当な拒否**で、planner が B2 / A20 から除外 → **ユーザー承認を得て司会が入れた** | **round 2** (REVIEW round 2 の minor 2 件): ADR に「★位置を測るときはマスクを収縮しない — 値を読むときとは逆」を**規則として**追加 (29 px の再発経路が耐久文書に無かった。機構 = reservoir が半解像度で debug 12 は LINEAR 拡大 / 13・14 は POINT。帰結 = 収縮 1 回で max 10 → 486・足跡外 0 → 169 画素で結論が反転)。`shot_verify.bat:348` の「分解する予定」を実態へ (grep が `M67h` だったため掛からなかった 6 か所目)。VERDICT round 2 OK (指摘なし)。coder の [追加] (`:346` の 1 語) は planner が実物 3 か所で裏取りして**スコープ内**と承認 — M67d 以降 `rt_refl.cs.hlsl:147` は `RtTraceRadianceFirstHit` を直接呼ぶので「共有しているのは Lod だけ」はもう事実でなく、片方だけ直すと隣り合う 2 文が矛盾する |

## レビュー
| round | 判定 | 深度/機能/視覚/品質 | 未解決 |
|---|---|---|---|
| 1 | PASS | 4 / 5 / 5 / 4 | minor 3 件 (全て文書の記述のみ。コード・golden・決定論に非影響)。blocker / major 0。reviewer は A1〜A19 を**自分で再実行**して全緑 + 実機を Win32 自動操作で 1400x861 と 1280x720 の 2 解像度で確認 (A5 / A6 / 4 階層目のドラッグ / Reset の往復)。受け入れ条件の外側 6 通りも突破。司会が名指しした 4 点 (Hero=16 の可逆性 / round 1 の古い結論の残存 / 誤読値の訂正漏れ / 枚数の整合) は**いずれも問題なし**で、唯一の齟齬が minor 3。全文は review-1.md。→ **司会の判断で 3 件とも閉じる (sub-03)** |
| 2 | PASS | 4 / 5 / 5 / 4 | 前回指摘 3 件は**全て解消**、悪化 0。reviewer は ADR の 9 数値を独立に再測して**1 つ残らず一致**。#3 は「**自分の第 1 案が誤りで却下が正しかった**」と `git log -S` で自ら裏取りした。新規 minor 2 件 → **司会の判断で閉じる (sub-03 round 2)**。全文は review-2.md |

## ユーザー判断
- (2026-09-08、司会が AskUserQuestion で確認。planner の `[ユーザーに聞ける]` 3 件。**全て裁定どおり**)
- Q1 S5 の確定値を誰が決めるか → **(ii) ヘッドレスの計測で確定する** (裁定どおり)。
  帰無仮説 = 現行の既定表のまま、規則が要求したときだけ 1 行変える。sub-02 の手順 1〜4 を全て回す。
  ユーザーは実機の確定値を持ち込んでいない = 計測が唯一の根拠。
- Q2 `spatial` の既定を on に反転するか → **off 維持** (裁定どおり)。M67 U7 の規則で決着済み
  (目標帯 = 音響デモの床で temporal 単独 0.181 < spatial on 0.255)。M67 台帳の U7 に残っていた
  「完了報告で提示する」はこれで回収済み。**以後蒸し返さない**。
- Q3 `visRay` の既定を on に反転するか → **off 維持** (裁定どおり)。コスト約 2.7 倍
  (WARP / frames 20 で restir 2.320 → visray 6.166 ms)、利得は光漏れ緩和のみで ADR-016 に既知制限。
- (2026-09-08、sub-02 の VERDICT で planner が新設した `[ユーザーに聞ける]` Q2)
- Q2 `Hero` の M 上限を 8 → 16 に上げるか → **16 へ上げる** (**裁定と逆**。planner の裁定は
  「上げない = 帰無仮説を破れなかったので据え置く」)。根拠として提示した数値: 動く反射像の
  フリッカーが 35.5〜38.9% 改善 (Ro 3.789→2.317 / R 4.343→2.802)、一方で遅れの増分は 2 つの
  指標のどちらでも決着しない (規則の指標で 0.042〜0.083 = 閾値 0.05 をまたぐ / 動きを分離した
  対照では 0.010 だがその対照は同区間で非単調 = 分解能不足)。**ユーザーはフリッカー改善を採り、
  決着しない遅れのリスクを引き受けた。** planner へ補足として送り、仕様を直させてから実装へ回す。

## 申し送り (セッション跨ぎ)
- **(完了時点の積み残し = なし)** `CLAUDE.md:109` は司会がユーザー承認を得て `2415b3f` に含めた。
- **★ 収縮の要否 (「値を読む」と「位置を測る」で逆) の正本は [ADR-016](../../docs/adr/ADR-016-restir-reflection.md)
  へ移した** (`73155a0`)。台帳はセッションで流れるので、以後この規則は ADR を見ること。
- **(reviewer 調べ → 後続、任意)** 「予定」と書かれた完了済み記述が**他マイルストーンに 5 件**残る。
  掃除するなら `grep -rn "予定" tools/ src/ docs/` から。M67 系ではないので M67h の宿題にしない。
- **(sub-03 → 後続)** 「文書 + bat の `rem` だけ」の修正の検証コストは **Release ビルド + shot_verify**
  で足りる (`selftest` / `replay_verify` は `git diff -- src assets` が空であることを根拠に省ける)。
- **(sub-02 → 後続)** `--rt-debug 12` を数値化するときは**飽和判定を 8bit の生値 231 で行う**
  (逆引き後の 0.978 で判定すると `M >= 16` が全部 15.65 に潰れる)。
- **(sub-02 → 後続)** `--rt-class-override 0` は**もう mCap 8 ではない** (Hero が 16 になったため)。
  ADR の掃引表を再現するには `kRtReflClassTable[0].mCap` を一時的に 8 へ戻す。
- **(sub-02 → 後続)** `kRtRestirMaxM = 32` が mCap の天井。Prop 行を上げたくなったらまずこの定数を疑う。
- **(ADR の未検証一覧 → 後続)** `Character` 行は `--acoustic-demo` の敵 2 種で実測できる (frame 120/121、
  14 run 程度)。今回は回転体で測った段差を転用した。
- **(CRLF 確認の罠 → 後続)** bat の線種を数えるのに `awk '!/$/'` を使わない — MSYS が text モードで
  `` を落とし**全行 LF に見える偽陽性**が出る。`tr -cd '' | wc -c` で数えること。
- (planner 2026-09-08) `spec.md` 確定。サブ 2 本 = sub-01 (review-1 minor 5 件) / sub-02 (S5 の確定)。
  **sub-01 は Q1 の回答に依存しない**ので、回答待ちでも着手できる (これが順序の理由。spec §6)。
- (planner 2026-09-08) **Q1 `[ユーザーに聞ける]`**: S5 の確定値を誰が決めるか。planner の裁定は
  「(ii) ヘッドレスの計測で確定。ただし帰無仮説 = 現行の既定表」。(i) ユーザーが実機の確定値を持っているなら
  sub-02 の手順 3〜4 を飛ばして焼くだけになる。(iii) minor 5 件だけで閉じるなら sub-02 を落とす。
  付随して Q1-a (`spatial` の既定を on にするか。M67 U7 の完了報告に対する回答が台帳に無い) と
  Q1-b (`visRay` の既定を on にするか) も 1 行ずつ聞ける。詳細は spec §7。
- (planner 2026-09-08) 裁定のうち後から効いてくるもの: **sub-06 の「一様 Prop が 1.8 倍良い」だけを根拠に
  mCap を上げてはいけない** (フリッカー指標は静止シーンで長い履歴を無条件に有利にする片側の軸。spec §2 S3)。
  **`radiusPx` / `taps` / `radiusAlphaRef` は焼かない** (spatial 既定 off ではシェーダが 1 タップも撃たない。S4)。
  **`--shot-every` は決定的撮影モードを外す** (`EngineLoop.cpp:704`) ので測定に使えない (S5)。
- (planner 2026-09-08) 副産物: review-1 の「未確認 = J ≠ 1 の temporal 経路」は**ヘッドレスで閉じられる**。
  `rdemo_spin` は roughness 0.45 < `kRtReflMaxRoughness` 0.6 = 自分自身が反射の受け面で、かつ回っている
  = 受け側の `P_prev ≠ P` (spec §2 S9 / sub-02 手順 1)。
- (planner 2026-09-08) コミットは 2 本: `M67h:` (minor) / `M67i:` (S5)。最終レビュー後の司会コミットは
  `M67 追補: レビュー完了 — ...` を推奨 (`M67: レビュー完了` = 8e4272e と衝突するため)。
- (planner 2026-09-08、**Q2 のユーザー回答を受けて**) sub-02 を **差し戻し (round 2)**。新規 `M67j` に
  分けない — round 1 が書いた ADR-016 §S5 / engine_spec §6.4 は「1 行も変えない」と書いてあり、
  **未コミットのまま**なので、先にコミットすると**その 1 コミットだけ内容が偽**になる (bisect の毒)。
  件名も `M67i: ReSTIR の既定表を 2 軸で測る — 4 行は規則で維持、Hero だけ M 上限を 16 へ` へ変更。
- (planner 2026-09-08、**回答から落ちる帰結。reviewer と完了報告で必ず触れること**)
  Hero を 16 にすると **`Character` (16) / `Default` (16) と同値**になり、**spatial が既定 off の間は
  クラスが選ぶのが `mCap` だけ**なので、**出荷構成では Hero / Character / Default の 3 クラスが同挙動**に
  なる (差が残るのは Vehicle 24 と Prop 32)。デバッグ 13/14 の色分けと、spatial を on にしたときの
  半径 / タップの差は残る。ADR に 1 段落 (A19) + selftest に `hero.mCap <= 他 4 クラスの最小値` (A16) で
  「次に Hero を 24 へ上げる事故」を機械で止める (現行は `hero < prop` しか見ていない)。
- (planner 2026-09-08、sub-02 round 2 の成果。**次に RT の M を測る人へ**)
  `--rt-debug 12` のヒートマップを数値へ戻すときは **飽和判定を 8bit の生値 231 で行う** —
  トーンマップ後の飽和は 255 ではなく 231 なので、逆引き後の値 (0.978) で判定すると
  `M > 16` の分岐が一度も発火せず **`M ≥ 16` が全部 15.65 に潰れる** (round 1 で踏み、round 2 で修正)。
  マスクを重ねるときは **debug 12 が LINEAR / debug 13・14 が POINT** で拡大される差にも注意。
  → **(M67k で耐久文書へ移した)** 「**値を読むときは収縮する / 位置を測るときは収縮しない**」の
  規則と、収縮 1 回で結論が反転する実測 (収縮なし max 10 px・足跡外 0 / 1 px 収縮 max 486 px・
  足跡外 169) は **ADR-016 の「1 行の変更が本当に絵へ届いたかの観測」節**が正本。
  飽和判定 231 の罠は同 A9 節。**この台帳ではなく ADR を読ませること** (台帳はセッションで流れる)。
- (planner 2026-09-08) **`--rt-class-override 0` はもう mCap 8 ではない** (Hero が 16 になったため)。
  ADR の掃引表を再現するには `kRtReflClassTable[0].mCap` を一時的に 8 へ戻す。
- ~~(planner 2026-09-08、未特定のまま残すもの) golden の差分 233 画素のうち 1 画素だけが
  Hero マスクから 29 px 離れており未特定。追跡しないと裁定した~~ →
  **(2026-09-08、review-1 #2 で撤回) 29 px は測定誤りで、そんな画素は存在しない。**
  reviewer の再測は max 10 px = A-Trous の足跡の内側。論証も原理的に強い — ADR は距離を
  「旧 ∪ 新」の Hero マスクから測ったと書いており、**和集合は新マスクの上位集合**なので
  距離は新マスク基準の値以下にしかならず、29 px は出得ない。**planner の「追跡しない」という裁定は
  蒸し返しではなく無効化**された (追跡対象が存在しなかった)。sub-03 (A21) で ADR / engine_spec を
  coder 自身の再測値へ訂正する。**round 3 でこの段落を ADR に入れさせたのは planner の指示なので、
  訂正の責任は planner にもある。**
- (planner 2026-09-08) **出荷構成で Hero の 1 行だけ上げた効果は 1 度も観測していない**
  (`--rt-class-override` は全インスタンスを同じクラスにするため)。round 2 の A17 / A18 がその観測。
  **golden がビット一致だったら `--update` せず止めて報告**させる — 「1 行変えたのに絵に届かない」なら
  golden の被写体選定を見直す話になる。
