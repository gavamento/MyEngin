# review-2: m67h-restir-finalize

- 日付: 2026-09-08
- 対象コミット範囲 (通算): `9a893cdeb1095b113c99b03b1af925afe1e36e3c..HEAD`
  - `6c8a1b5` M67h: レビュー minor 5 件の回収 — 触れないチューニング UI と黙って落ちる reflectionClass
  - `30ce7ab` M67i: ReSTIR の既定表を 2 軸で測る — 4 行は規則で維持、Hero だけ M 上限を 16 へ
  - `2415b3f` M67j: 衛生 — 「M67h でやる」の残骸を決着済みへ、golden 差分の実測値を訂正 (**今回の差分**)

```
REVIEW: PASS
round: 2
軸 (1-5):
  製品の深度: 4 — round 1 で 4 に落とした理由 (「次の人を誤らせる記述」が M67h 自身の周辺に残る) は
    5 か所とも解消した。特に #2 の訂正は**第三者が再現できる形**になっており、私が独立に測った
    8 つの数値 (チェビシェフ 10 / ユークリッド 11.2 / マンハッタン 15 / 1px 収縮 486・足跡外 169 /
    最大連結成分 485・同 185 / debug 13 は 328・同 151 / 13∪14 は 10 / 連結成分 56) が
    **ADR の記述と 1 つ残らず一致**した。5 にしないのは、(a) 距離を測るマスクを**収縮しては
    いけない理由**が耐久文書 (ADR) に無く、収縮すると結論が反転する (max 10 → 486、足跡外 0 → 169 画素)、
    (b) 同じ「完了済みを未来形で書いた」残骸が `shot_verify.bat:348` に 1 件残る (M67h ではなく
    M67 表記なので A20 の grep に掛からなかった) の 2 点。どちらも手順を誤らせる種類ではない。
  機能性: 5 — B1〜B7 を自分で全て再実行して緑。Debug / Release とも `/p:MyeWarnAsError=true` で
    警告 0 / エラー 0、`--selftest` exit 0 (`FAIL:` 0 行 / `PASS: heroIsMinCap` あり)、
    `check_rules.ps1` 0/0、**`shot_verify.bat` 24 枚すべて maxDiff=0 diffPixels=0** (= 書き換えた
    bat が CRLF 事故を起こしていないことの実行証明)、`git status` は `tests/` に変更 0 件。
    回帰は 1 件も見つからなかった。
  ビジュアルデザイン: 5 — 今回の差分は **`src/` の非コメント行が 0 行**
    (`git diff 30ce7ab..HEAD -- src/` からコメント行と空行を除くと増減 0) なので UI へ届く経路が無く、
    round 1 の実機確認 (`g\06_tuning.png` / `g\07_classtable.png` / `g\09_after_drag.png` /
    `g\11_after_reset_table.png` / 720p の 3 枚) がそのまま有効。ビルドと selftest も緑。
  コード品質: 4 — 触ったのはコメント / 文書 / bat の `rem` だけで、コード意味論の変更 0 を
    diff で確認した (= `replay_verify` 未実行という coder の判断は妥当)。bat は blob が LF /
    作業ツリーが CRLF 464 行 / BEL 0 バイトで `.gitattributes` の規約どおり。
    4 に留めるのは、ADR が「収縮せずに使う」と**結論だけ**書いて**理由 (debug 12 は LINEAR 拡大
    なので M の読み出しでは収縮が要る / debug 14 は POINT なので距離では収縮してはいけない)** を
    書いておらず、そこが今回訂正した誤りの再発経路そのものだから (指摘 1)。

指摘:
  1. [minor] 宛先: coder — **距離を測るマスクを「収縮してはいけない」理由が ADR に無い。
     収縮すると結論が反転するので、次に再測する人が同じ誤りに落ちる経路が開いたまま。** —
     根拠: `docs\adr\ADR-016-restir-reflection.md:498-500` は「マスクは `--rt-debug 14` が Hero と
     言う画素 (frame 40 / 400 px) を**収縮せずに**使う」と手順を書くが、**なぜ収縮しないのか**が
     どこにも無い。一方 `:484` の A17 の表は同じ Hero マスクを「**1 px 収縮、20 px**」で使っており、
     読者からは「同じマスクなのに片方だけ収縮する」理由が読めない (ADR 全文に `LINEAR` /
     `POINT` の語が 0 件。理由が書いてあるのは `plans\m67h-restir-finalize\sub-02.md` の
     round 2 実装メモと `harness.md` の申し送り = harness の作業記録であって耐久文書ではない)。
     影響の実測 (本レビューで計測、現行 Release / frame 40):
       - 収縮なし (400 px) … 中央値 1 / p95 4 / **max 10** / 足跡 (±12 px) の外 **0 画素**
       - 8 近傍 1 px 収縮 (20 px) … 中央値 20 / p95 146 / **max 486** / 足跡の外 **169 画素**
     つまり収縮を 1 回入れるだけで「全画素が足跡の内側」という ADR の結論が**反転する**。
     `:511` の否定リストに「1 px 収縮したマスク」が挙がってはいるが、そこは 29 px の**原因候補**の
     文脈で、「距離では収縮するな」という規則としては読めない —
     期待: `:498` のバケットに 1 行足す。文面は既に `sub-02.md` にある
     (「reservoir は半解像度で **debug 12 が LINEAR 拡大**なので M を読むときは境界画素が
     近傍と混ざる = 収縮が要る。**debug 13 / 14 は POINT 拡大**なのでクラスマスクは正確 =
     距離では収縮してはいけない」)。ADR へ写すだけで閉じる。

  2. [minor] 宛先: coder — **同種の「完了済みを未来形で書いた」記述が `tools\shot_verify.bat:348` に
     1 件残っている** (優先度は低い。手順を誤らせる種類ではない) —
     根拠: `rem   M67 は実際にこのヘルパを first-hit 版へ分解する**予定なので**、GI 側が本当に要る。`
     — 分解は既に完了している (`assets\shaders\rt_common.hlsli:568` に `RtTraceRadianceFirstHit`、
     `:622-626` の `RtTraceRadianceLod` はそれを呼ぶだけのラッパ)。**A20 の grep が `M67h` だった
     ため掛からなかった 6 か所目**で、私も round 1 で `M67h` しか grep しておらず見落とした
     (自分の検査の穴)。coder が直した `:378` の 30 行上、同じ RT 節の中にある。
     **文の結論 (GI 側の 1 枚が要る) は今も正しい**ので実害はほぼ無い —
     期待: 「M67 でこのヘルパを first-hit 版へ分解した (rt_common.hlsli:568) ので、GI 側が本当に要る」
     程度の 1 行。なお `予定` の同種は他マイルストーンにも 5 件ある
     (`AcousticDebugDraw.cpp:24` / `UINav.h:5` / `EngineAPI.h:492` / `TerrainPass.h:24` /
     `TerrainAsset.h:15`) が、**それらは M67 系ではないので本件の宿題にしない** (掃除を広げる提案ではない)。

検証した手段:
  - 読んだ範囲: `sub-03.md` 全文 (B1〜B7 + 実装メモ + B2 の全件台帳 + フィードバック履歴) /
    `spec.md` の §2 S11′・§5 A20〜A22 の追記 / `harness.md` / `2415b3f` の diff 全ファイル /
    ADR-016 の S5 節 (:476-520) と訂正段落 (:504-516) / `engine_spec.md:510-522`・`:1894-1949` /
    `CLAUDE.md:106-112` / `tools\shot_verify.bat:335-385`
  - ビルド: MSBuild Debug / Release とも `/p:MyeWarnAsError=true` → exit 0、`warning` 0 行 / `error` 0 行
  - `bin\x64\Debug\Editor.exe --selftest` → exit 0、`  FAIL:` **0 行**、`ALL PASS` 32 行、
    `PASS: heroIsMinCap` あり
  - `pwsh -File tools\check_rules.ps1` → 0 error / 0 warning
  - `tools\shot_verify.bat` → `[PASS] screenshot regression (24 shots …)`、
    `=== shot:` 24 件 / `maxDiff=0` 24 件 / `img-diff] PASS` 24 件、`git status` の `tests/` は 0 件
    (**B1 = 書き換えた bat が完走することの実行証明**)
  - bat の線種をバイト単位で確認 (司会の警告どおり `awk '!/\r$/'` は使わず `tr -cd`):
    committed blob = **CR 0 / LF 464** (LF)、作業ツリー = **CR 464 / LF 464** (完全 CRLF)、
    **BEL 0 バイト**、`.gitattributes:9` の `*.bat text eol=crlf` と整合。
    9a893cd / 30ce7ab の blob も CR 0 なので**線種の扱いは変わっていない** (461 → 464 行 = +3 行)
  - コード意味論の非変更: `git diff 30ce7ab..HEAD -- src/` からコメント行・空行を除くと**増減 0 行**
    → `replay_verify` を回さない判断は妥当 (sub-02 が同一意味論で全 10 ジョブ緑)
  - **#2 の訂正を第三者として再現** (現行 Release で撮り直した `tests\actual\r2_d14_f40.png` /
    `r2_d13_f40.png` + `git show 9a893cd:tests/golden/demo_render_rtrefl_restir.png` と現行 golden):
    | ADR の記述 | 私の独立計測 | 一致 |
    |---|---|---|
    | 233 画素 / maxDiff 5 / 最悪 (367,372) R 64→69 | 233 / 5 / (367,372) R 64→69 | ✓ |
    | `--rt-debug 14` の Hero = 400 px | 400 px (色 4 種のみ) | ✓ |
    | 収縮なしで 中央値 1 / p95 4 / max 10、足跡外 0 | 同じ | ✓ |
    | 最遠 (621,225) 距離 10 / チャンネル差 1 (R 44→45) | 同じ | ✓ |
    | ユークリッド 11.2 / マンハッタン 15 | 11.2 / 15 | ✓ |
    | 1 px 収縮 (20 px) → 486、足跡外 169 画素 | 8 近傍収縮で 20 px → 486 / 169 | ✓ |
    | 最大連結成分 → 485、足跡外 185 画素 | 485 / 185 (成分数 56、最大 60 px) | ✓ |
    | `--rt-debug 13` の Hero (3081 px) → 328、足跡外 151 | 3081 px → 328 / 151 | ✓ |
    | 13 ∪ 14 は 10 (14 単独と同値) | 10 | ✓ |
    → **8 通りの否定材料まで含めて全数値が再現**。上界の論証 (和集合は上位集合 → 距離は以下)
    も 13∪14 の実測 (max 10) で裏が取れている
  - **#3 の裁定 (私の第 1 案の却下) を自分で裏取り**: `git log -S "and twenty-two images today"` →
    追加は `8994b1d` (M68c)。その時点の `shot_verify.bat` は `call :shot` が **22 本**で、
    そこに `demo_render_rtrefl` / `_rtgi` / `_rtrefl_restir` (追加は `73a439e` = M67a) が含まれ、
    それらは `AcousticField` の初出 `c76cf82` (M65a) より**後**。つまり
    「then 存在した 22 枚が predate する」は偽 = **私の第 1 案は誤りで、planner の却下が正しい**。
    採用された第 2 案 (`the suite is seven pairs and twenty-four images today`) は
    「今日のスイートの総数」としか読めず、`:1895` の史実記述 (twenty-two / across M68) は未接触
  - grep 全件: `M67h` / `M67i` / `M67j` の plans 外 35 件を目視し、**「これからやる」と読める記述 0 件**
    (残りは全て過去形の由来・完了報告)。`CLAUDE.md:110` も「S5 は M67h で決着済み = off 維持」に
    なっている。`S5` の 19 件も台帳どおり (別マイルストーンの S5 = Source Control / 音響は無関係)
  - 見た画像: `tests\actual\r2_d14_f40.png` / `r2_d13_f40.png` / 新旧 `demo_render_rtrefl_restir.png`
    (数値化のみ。UI は round 1 の 12 枚が有効 — 上記のとおり非コメント行 0 のため)

前回指摘の消込:
  1. (「M67h でやる」の残骸、宛先 coder) — **解消**。私が挙げた 4 か所すべてと、coder が
     grep の全件列挙で自分で見つけた 5 か所目 (`ADR-016:180` の「所見: S5 で最初に触るノブは
     M 上限」→ `:184-186` に「この所見は当たった + 1.8 倍だけを根拠に上げてはいけない」を追記) が
     直っている。最も実害があった `shot_verify.bat:378` は「M67i で差し替え済み / **次も --update は
     使わない** (24 枚を一括で撮り直すと『1 枚しか動いていない』を git status で示せない) /
     実物を 1 枚コピーし、コピー後にもう一度この bat を回す」へ = **私の指摘より強い記述**に
     なっている (再検証の手順まで書いてある)。`EditorApp.cpp:1262` / `RtTypes.h:276-278` /
     `CLAUDE.md:110` も現在形。`RtTypes.h` は「再評価できるようにしておく」という**設計意図を
     残したまま**時期の指定だけ落としており、私の期待どおり。
     ※ 同種の 6 か所目 (`shot_verify.bat:348`、M67 表記) を今回新たに見つけたので指摘 2 として
     立てた。これは #1 の再発ではなく**私と A20 の grep が同じ穴を持っていた**もの。
  2. (29 px が再現しない、宛先 coder) — **解消**。ADR:490-516 が再測値へ全面差し替えされ、
     「未特定」「追跡しなかった」「8 px 以内」の 3 概念が消えて、距離の定義 (チェビシェフ /
     debug 14 の Hero 400 px / 収縮なし) と分布 (中央値 1 / p95 4 / max 10) と
     「233 / 233 が足跡 (±12 px) の内側」に置き換わった。**coder は私の数値を写さず独立に再測し、
     3 つ目の測定 (私) とも完全一致**。29 の出所を 8 通り試して再現しなかったことと
     「原因未特定」も明記されており、上界の論証で結論が保証されている。
     `engine_spec.md:518-520` も同じ値へ追随。**残る改善余地は「収縮するな」の理由だけ** (指摘 1)。
  3. (主語と数の不一致、宛先 planner) — **解消**。ただし**私の第 1 案は誤りで、却下が正しかった**
     (上記の git 裏取り)。採用された第 2 案で括弧が「今日のスイートの総数」としか読めなくなり、
     数字 24 は据え置き、`:1895` の史実は未接触。planner が `spec.md` §2 に S11′
     (「現在形か史実かの判定は主語まで見る。数だけ差し替えない」) を足して再発経路も塞いでいる。
  (悪化: 0 件。golden 24 枚 maxDiff=0 / selftest / check_rules / 両構成の警告 0 で確認)
```

## 補足 (指摘に落とさなかった確認事項)

- **司会の重点 3 点への回答**:
  1. *訂正が第三者の測定で再現するか* → **する**。ADR 本文だけを読んで手順を組み、9 項目
     (本表) が完全一致した。距離の定義・マスクの出所 (`--rt-debug 14` / frame 40 / 400 px) ・
     収縮の有無は本文から一意に決まる。**足りないのは「なぜ収縮しないか」だけ** (指摘 1)。
  2. *bat の CRLF* → 作業ツリー CR 464 / LF 464、BEL 0、`shot_verify.bat` 完走 24 枚緑。
     committed blob は LF (`.gitattributes` が checkout で CRLF 化する規約どおり) で、
     9a893cd / 30ce7ab と扱いが変わっていない。
  3. *新しく触った 5 か所が「これからやる」と読めなくなっているか* → **なっている** (5 か所とも
     過去形 + 現在の出所の明示)。`RtTypes.h` は設計意図を残した点、`shot_verify.bat` は
     「コピー後にもう一度 bat を回す」まで書いた点が良い。
- **蒸し返していないもの**: Hero=16 のユーザー判断 / 旧 (Hero=8) マスクを撮らなかったこと /
  29 px の原因が未特定であること / `RtRestirCB` の `pad0`・`pad1` の並び / sub-03 で
  `replay_verify` を回していないこと。最後の 1 件については、`git diff` のコメント除去で
  **非コメント行の増減 0** を自分で確認したので、未検証扱いにはしない。
- **CLAUDE.md:110** は司会がユーザーの承認を取って手で入れた旨の報告を受けているが、
  reviewer が確認できるのは**成果物としての 2 行の差分が正しいこと**まで (承認の有無は
  エージェント間のメッセージでは判定できない)。差分の内容自体は他 4 か所と同じ「決着済み」化で、
  記述として正しい。
