# sub-02: S5 の確定 (2 軸の計測 → 規則適用 → 既定値と文書)

- 依存: sub-01
- 状態: **OK (round 3、コミット待ち)** — round 1 OK (未コミット) → §7 Q2 のユーザー回答で差し戻し →
  round 2 OK (should 1) → round 3 で should を回収
- 往復: 2

---

## ★round 2 でやること (ここだけ読めば差分が分かる)

**round 1 の成果 (64 run の測定・ADR の表・engine_spec・A9 の観測) は 1 バイトも捨てない。**
変えるのは「結論」と、そこから落ちる 1 行・selftest・golden だけ。

`spec.md` §7 Q2 に**ユーザーの回答が入った**: **`Hero` の M 上限を 8 → 16 に上げる**。
planner の裁定 (据え置き) と**逆**で、ユーザーはフリッカー 35.5〜38.9% の改善を採り、
2 つの指標のどちらでも決着しなかった遅れのリスクを引き受けた。**この判断は確定。蒸し返さない。**

なぜ新しいコミット (M67j) に分けず差し戻しにするか: round 1 が書いた ADR-016 §S5 と
engine_spec §6.4 は「結論は 1 行も変えない / 全 5 行維持」と書いてある。**まだコミットしていない**
ので、これを一度コミットしてから次のコミットで書き直すと、**その 1 コミットだけ内容が偽になる**
(bisect / 履歴を読む人が誤る)。知っている今のうちに 1 コミットへ畳むのが正しい。

### r2-1. 値と機械固定

- `RtTypes.h` の `kRtReflClassTable[0].mCap` (Hero) を **8.0f → 16.0f** (1 行)。
  他の 4 行・`radiusPx`・`taps` は**触らない** (測定は「維持」を支持しており、ユーザーが覆したのは Hero だけ)。
- `RtSelfTest.cpp` の既存の不変量は**そのまま通る**ことを確認する
  (`hero.mCap < prop.mCap` は 16 < 32 で成立、`character.mCap < vehicle.mCap` は無関係)。
- **★新しい不変量を 1 本足す**: `hero.mCap <= 他 4 クラスの mCap の最小値`
  (= 主役が「最も保守的」であることの本体)。この変更で **Hero (16) は Character / Default (16) と
  同値 = 意味論の境界ちょうど**まで来たので、次に誰かが Hero を 24 に上げると
  「主役が中立より積極的」という真逆の設定が無検査で通ってしまう。等号は許す (`<=`)。
- `RtTypes.h` / `RtSelfTest.cpp` の据え置きコメント (round 1 で書いたもの) を書き直す:
  **「4 行は規則で維持、Hero だけユーザー判断で 16」**と、**却下した測定値ごと**残す
  (規則の指標で +0.083 / +0.056 / +0.053 / +0.042 = 4 標本中 3 つが閾値 0.05 超 → 規則は不成立だった、
  ただし軸 A は 35.5〜38.9% 改善、動きを分離した対照では 0.010 = 分解能内)。**後から差し戻せる形**にする。

### r2-2. 変更が本当に絵へ届いたかを観測する (ここが round 2 の主眼)

`--rt-class-override` の掃引は「全インスタンスを同じクラスにする」ので、
**出荷構成 (クラス混在) で Hero の 1 行だけを上げた効果は、まだ 1 度も観測していない。**

- **M の観測**: frame 40 の `--rt-debug 12` を round 1 の手順 (PNG → M の数値化 + frame 1/2/3 の
  ラダー校正) で読み、**`--rt-debug 14` の Hero マスク (Ro / R = 静止した受け面に映った回転体)** で
  M の頭打ちが **8 → 16 へ動く**ことを示す。
  ★**測る領域を間違えないこと** — `--rt-debug 13` の Hero (= 回転体**自身**の面 = H) は
  *映っているもの* が Default なので **M は 16 のまま動かない**。効くのは「Hero が**映っている**画素」。
- **golden の差分の形**: 新旧 `demo_render_rtrefl_restir.png` を `--img-diff` で比べ、
  **maxDiff と差分画素数**、および**差分画素が Hero マスクに収まっていること**を示す。
  - 予測 (先に書いてから測る): 差分は**数百画素の桁** (Ro 322 / R 384 px は `--rt-debug 11` の
    全画面表示での数字なので、合成後の本編でも同じオーダー)。**全画面が動いたら別の何かが動いている**
    ので `--update` せずに報告する。
  - **★ビット一致だったら止まって報告する。** それは「1 行変えたのに出荷構成の絵に届いていない」
    = Hero クラスが golden の画に 1 画素も出ていないという重大な発見で、golden の被写体選定
    (A12 の主張) を見直す話になる。黙って「変化なし」で通さない。

### r2-3. golden

`demo_render_rtrefl_restir.png` (frame 40、tol=0、`--frames 41 --shot-frame 40`、
`--warp --no-audio --font-embedded --width 960 --height 540 --no-fxaa`、
`--render-demo --deferred --rt-refl --rt-restir`) を撮り直す。

- **`shot_verify.bat --update` を使わない。** M67 sub-07 と同じ流儀で、比較 run の実物を
  **1 枚だけコピー**する。`git status` が「`tests\golden\` の変更は 1 ファイル」を示すことが直接の証拠。
- 独立した 3 run が `--img-diff --tol 0` で一致すること。
- 撮り直し後に `shot_verify.bat` が **24 枚全緑**。

### r2-4. 文書 (結論の書き直し)

- **ADR-016 「S5 の結論」節**: 表・領域の定義・健全性検査・R2〜R6・A9・未検証一覧は**そのまま**。
  書き直すのは結論の枠だけ:
  - 見出しと導入を「2 軸で測って、**4 行は規則どおり据え置き、Hero だけユーザー判断で 16 へ上げた**」に。
  - 「行への割り当て」表の Hero 行を「**規則: 不成立 → ユーザー判断で 16**」に。
    **規則が不成立だった数値を消さない** (後から 8 へ戻すときの根拠になる)。
  - **ユーザーが引き受けたリスクを明記**: 遅れの増分は 2 つの指標のどちらでも決着していない
    (規則の指標 0.042〜0.083 / 動きを分離した対照 0.010、対照は同区間で非単調 = 分解能不足)。
  - **★新しく書く 1 段落**: この変更で **`Hero` (16) が `Character` / `Default` (16) と同値**になり、
    **spatial が既定 off の間はクラスが選ぶのが mCap だけ**なので、出荷構成では
    **Hero / Character / Default の 3 クラスが同じ挙動**になる (差が残るのは Vehicle 24 と Prop 32)。
    デバッグ 13 / 14 の色分けと `radiusPx` / `taps` の差は残るので、spatial を on にすれば 5 段に戻る。
    これは判断を蒸し返すためではなく、**次に表を触る人が知っていなければならない事実**として書く。
  - 「golden も 1 枚も動いていない」という round 1 の記述を、撮り直した事実と差分の形へ。
- **engine_spec §6.4**: round 1 の「The default table was measured, not guessed」の段落を、
  「規則では 5 行とも不成立 → 4 行は据え置き、Hero だけ実機の判断で 16 へ」に。
  golden を 1 枚撮り直したことも 1 節。
- 「既知の制限」節の「S5 は M67h で計測して決着。1 行も変えない」も実態へ。
- **`--rt-class-override` の掃引値 (8/16/24/32) の説明はそのまま**でよい (掃引は override の話で、
  既定表の話ではない)。ただし Hero 行の既定が 16 になったことで「override 0 = mCap 8」は
  **もはや既定表の Hero と一致しない** ので、そう読める箇所があれば直す。
- **数値の埋め込みを探す**: `grep -rn "8\.0f" ` ではなく、`kRtReflClassTable` の 5 行を文章で写している
  箇所が他に無いかを README / engine_spec / ADR で確認して報告する (planner の見立てでは
  `RtTypes.h` 以外に表を書き写した場所は無い)。

---

## 入力が 2 通りある (round 1 の記述。回答は「(ii) 計測で確定」だった)

| 入力 | やること |
|---|---|
| **(既定) ユーザーの確定値が無い** | 下の「手順」を最初から回す。**帰無仮説 = 現行の既定表**で、spec §4.4 の規則 R1〜R6 が要求したときだけ値を変える |
| **ユーザーが実機の確定値を持っている** (Q1 の回答が (i)) | 手順 3〜4 (本測定と規則適用) を**飛ばして**ユーザー値を焼く。手順 1 (A9 の観測) / 5 (golden) / 6 (文書) は**そのまま実施**する。ユーザー値は計測に優先する。値の出所を「ユーザーの実機判断 (日付)」として ADR に明記する |

司会から追加指示が来ない限り (既定) で進める。**「回答待ち」で止まらない。**

## やること

`spec.md` §4.4 の R1〜R8 が仕様。以下は「どの順に潰すか」だけを決めたもので、
**最もリスクの高い未知 (= 指標が立つのか) を最初に潰す**順序になっている。

### 手順 0 — 基準線

Release ビルド (`/p:MyeWarnAsError=true`) を作り、`shot_verify.bat` が 24 枚全緑であることを
**測定を始める前に**確認する。ここが赤いまま測ると、後で「既定値のせいか sub-01 のせいか」が切り分けられない。

### 手順 1 — 領域の決定と、review-1 の「未確認」を閉じる (A9)

`--render-demo --deferred --rt-refl --rt-restir` の frame 40 で、

- `--rt-debug 13` (一次ヒットのクラス) の **赤 = Hero** 画素 = **回転体 `rdemo_spin` 自身の表面**。
  これが「動いている受け面」= `P_prev ≠ P` = **J ≠ 1 の temporal 経路**。
- `--rt-debug 14` (反射像側のクラス) の **赤 = Hero** 画素 = **回転体が映っている受け面**。
  これが軸 B (追従率) の測定領域。

この 2 つのマスクから矩形を機械的に決め (M67 A7-a と同じ流儀)、実装メモに矩形の座標と決め方を残す。

**A9 の主張**: `--rt-debug 12` (reservoir の M) の frame 40 で、上の「回転体自身の表面」画素の M が
**1 より大きい** (履歴が動く受け面を跨いで生き残っている = 厳密 Jacobian と velocity の配線が
GPU で機能している) こと。あわせて同領域に孤立高輝度画素 (firefly) が無いこと
(M67 sub-05 と同じ「最大輝度の on/off 比較」で可)。

- 予測を先に書くこと: 「M は 1 より大きいが cap (Hero = 8) までは伸びない」か「cap まで伸びる」か。
  予測と違ったら、**測定を進める前に**何が違うのかを調べる (spec §7 のリスク)。
- M が 1 のまま = J が常に棄却されている = review-1 の心配が的中したケース。その場合は
  **測定を止めて planner へ「不安・質問」で上げる** (S5 より先に直すべきバグの可能性)。

### 手順 2 — 軸 B (追従率) の健全性検査

本測定に入る前に、**指標が mCap に反応するか**だけを 2 条件で確かめる。

```
軸 B = mean|test[41] - test[40]| / mean|ref[41] - ref[40]|   (手順 1 の矩形、--rt-debug 11 = 反射レーン)
  ref  = --rt-refl            (ReSTIR off、デノイズは既定のまま)
  test = --rt-refl --rt-restir --rt-class-override N
```

- 健全性 (1): **静止した反射像の領域**では ref の分母がノイズ主体になり比が意味を失う → 分母が
  十分大きい (= 本当に動いている) 領域を選べているかを数値で示す。
- 健全性 (2): `--rt-class-override 0` (mCap 8) と `3` (mCap 32) で軸 B が**目に見えて違う**こと。
  違わなければ「この条件では mCap は追従に効かない」= R1 の「維持」で閉じてよい (手順 3 を短縮できる)。
- ここで使う 4 枚 (ref 40/41 + test 40/41 ×2) は本測定の一部なので**無駄にならない**。

### 手順 3 — 本測定 (2 軸 × 4 条件)

**最小セット**: `{off, override 0 (mCap 8), override 1 (16), override 3 (32)}` の 4 条件 × frame 40/41。

- **軸 A (フリッカー)**: `--rt-anim-seed --rt-no-temporal --rt-no-svgf --rt-debug 11`
  (M67 A6/A7 と同一手法。`--rt-no-temporal` は SVGF 側だけを止め、ReSTIR の temporal は止まらない)。
- **軸 B (追従率)**: `--rt-anim-seed --rt-debug 11` (デノイズ既定のまま = 端から端までの応答)。

`override 2` (mCap 24) と「既定混在 (override 無し)」は、**4 点で結論が割れたときだけ**足す
(情報の価値 ÷ 取得コストで並べる)。

- 表は「条件 × (軸 A、軸 B)」の形で実装メモへ。M67 の既存数値 (音響デモの床: temporal 単独 0.181 /
  一様 Prop 0.100 / spatial on 0.255) との**比較可能性は主張しない** (被写体もフレームも違う) —
  比較するなら被写体を揃えて測り直す。
- **`--shot-every` を使わない** (spec §4.4 R8。`EngineLoop.cpp:704` で決定的撮影モードが外れる)。
  フレームは `--frames 41 --shot-frame 40` / `--frames 42 --shot-frame 41` の個別 run。
- **A12**: 代表 1 条件を 2 回撮って `Editor.exe --img-diff A B --tol 0` が PASS すること。
- 画像は `tests\actual\` (gitignore) へ。reviewer が再撮影できるようコマンドを実装メモに残す。

### 手順 4 — 規則の適用 (spec §4.4)

- **R1**: 変更条件 (軸 A が 20% 以上改善 **かつ** 軸 B の低下 0.05 未満) を満たしたクラスの `mCap` だけを
  `kRtReflClassTable` で変える。満たさなければ**現行維持**。
  どちらの結論でも「どの数値がどの規則をどう満たした / 満たさなかったか」を書く。
- **R5**: 軸 B が **0.8 未満**だったときに限り、`svgfHistory` 8 → 4 の 1 候補を再ビルドして軸 A/軸 B を測り直す。
  軸 B が上がり軸 A が悪化しないときだけ焼く (このとき `RtReflRestirParams::svgfHistory` の初期化子は
  新しい定数を `RtTypes.h` に作って指す — `RtSelfTest.cpp:1192` の
  `def.svgfHistory == kRtReflMaxHistory` も一緒に直す)。0.8 以上なら**やらない**。
- **R2 / R3 / R4 / R6**: 変えない。ADR に理由を書く (R4 は「spatial を on にしたときの初期値であって
  確定値ではない」と明記)。
- **Q1-a / Q1-b がユーザーから返ってきた場合**: `spatial` / `visRay` の既定を 1 行変え、
  `RtSelfTest.cpp:1197` の `TEST_CHECK(def.spatial == 0 && def.visRay == 0 ...)` と、その直前のコメント
  (「ここを 1 に戻すなら計測をやり直すこと」) を**ユーザー判断であると分かる形**に書き直す
  (計測結果を消さない — 「計測は temporal 単独が良かったが、実機の主観で on を選んだ」と両方残す)。

### 手順 5 — golden (値を変えたときだけ)

`demo_render_rtrefl_restir.png` を撮り直す。**`--update` で塗り潰さない** — M67g と同じく
比較 run の実物をコピーし、`git status` が「1 枚しか動いていない」ことの直接の証拠になる形にする。
3 回の独立 run が tol=0 で一致することを示す。値を変えなかったら golden は 1 枚も触らない。

### 手順 6 — 文書 (値を変えなくても必ずやる)

- **ADR-016** に「S5 の結論」節: 計測表 (条件 × 軸 A/B) / 変えた値と根拠 / **変えなかった値と理由** /
  **未検証のまま残る条件** (カメラが大きく動く条件の firefly と二重 temporal の主観品質。
  ヘッドレスに経路が無く、デモには触らないと決めたため)。A9 で閉じた「J ≠ 1 の経路」も書く。
- **engine_spec.md** §6.4 の該当箇所。加えて**現在形の golden 枚数だけ**を実測 (24) に合わせる
  (`:1926` の "and twenty-two images today" / `:1995` の "22 deterministic screenshots" /
  `:2041` の "all fourteen other" が候補)。**`:1872` のような史実の記述は触らない**
  (触るなら「M67 時点で」と主語を補う)。直した行を実装メモに列挙する。
- **CLAUDE.md**: 既定値を変えたときだけ (golden の frame や既定の説明が変わる場合)。
- `RtTypes.h` の変えた定数のコメントに「いつ・何を測って決めたか」を 1〜2 行
  (次にここを触る人が、また同じ測定をやり直さずに済むように)。

## やらないこと (このサブでは)

- **デモシーン / デモ材質 / カメラに触る** (spec §4.4 R7)。golden 24 枚の被写体そのもの。
- `spatial` の既定 on/off の再評価 (U7 で決着済み。ユーザーが指示した場合のみ 1 行)。
- `--img-diff` の矩形オプション追加、新しいデバッグ CLI の追加 (spec §2 S12)。
- `radiusPx` / `taps` / `radiusAlphaRef` を焼くこと (spec §2 S4)。
- 再ビルドを伴う総当たり sweep (R5 の 1 候補だけが例外)。
- `kRtRestirWMax` の予防的追加 (spec §2 S8)。

## 触る場所 (planner の見立て)

| ファイル | 何を |
|---|---|
| `src/Engine/Renderer/RayTracing/RtTypes.h` | `kRtReflClassTable` (:233-239) / `RtReflRestirParams` (:246-262)。**規則が要求したときだけ**。触ったら根拠コメント |
| `src/Engine/Engine/RayTracing/RtSelfTest.cpp` | `:1180-1198` の既定値固定 (`tableOk` / `svgfHistory` :1192 / `spatial` :1197)。定数を変えたら一緒に |
| `docs/adr/ADR-016-restir-reflection.md` | 「S5 の結論」節を追加 |
| `engine_spec.md` | §6.4 / 現在形の golden 枚数 |
| `tests/golden/demo_render_rtrefl_restir.png` | 値を変えたときだけ |
| `CLAUDE.md` | 値を変えたときだけ |
| 一時スクリプト | `scratchpad` 配下 (リポジトリに残さない)。画像は `tests\actual\` |

## 受け入れ条件 (このサブ)

1. **A9**: 回転体表面の M > 1 と firefly なしを数値で示し、review-1 の「J ≠ 1 は GPU で未観測」を閉じた
   (または閉じられない理由を報告した)。
2. **A10**: 軸 B の健全性を**本測定より先に**確認した記録がある。2 軸 4 条件 (以上) の表がある。
3. **A11**: 変えた値・変えなかった値の**全部**に「どの規則がどの数値で要求した / 要求しなかったか」が付いている。
   憶測で変えた値が 1 つも無い。
4. **A12**: 代表 1 条件の 2 run が `--img-diff --tol 0` PASS。
5. **A1**: `shot_verify.bat` 24 枚全緑。値を変えたなら `demo_render_rtrefl_restir` の 1 枚だけが差し替わり、
   `git status` が他 23 枚の不変を示す。3 run tol=0。
6. **A13**: ADR-016 / engine_spec が更新され、**未検証のまま残る条件が明記**されている。
7. **A14**: `tools\replay_verify.bat` 全緑 (M67h 全体の sim 非接触の担保)。
8. **A15**: Debug / Release とも `/p:MyeWarnAsError=true` で警告 0、`check_rules.ps1` 0/0。

### round 2 で足すもの

9. **A16 (値と機械固定)**: `kRtReflClassTable[0].mCap == 16.0f` で、他 4 行・`radiusPx` / `taps` は不変
   (`git diff` が 1 行 + コメントだけであること)。`RtSelfTest.cpp` に
   **`hero.mCap <= 他 4 クラスの mCap の最小値`** の `TEST_CHECK` が増えている (等号可)。
   → `Editor.exe --selftest` 緑 + `git diff` の目視。
10. **A17 (変更が絵へ届いた証拠)**: frame 40 の `--rt-debug 12` を数値化し、
    **`--rt-debug 14` の Hero マスク (Ro / R)** で M の頭打ちが **8 → 16** へ動いたことを数値で示す。
    ★`--rt-debug 13` の Hero (回転体自身の面) は候補が Default なので**動かないのが正解** —
    両方を出して取り違えていないことを示す。
11. **A18 (golden の差分の形)**: 新旧 `demo_render_rtrefl_restir.png` の `--img-diff` の
    **maxDiff / 差分画素数**を報告し、差分が Hero マスクに収まっていることを示す。
    **ビット一致だったら `--update` せずに止めて報告する** (「1 行変えたのに出荷構成の絵に届いていない」
    = golden の被写体選定を見直す話。黙って通さない)。全画面が動いた場合も同じく報告してから進む。
12. **A19 (文書が実態と一致)**: ADR-016 §S5 と engine_spec §6.4 の結論が
    「4 行は規則で維持 / Hero だけユーザー判断で 16」になっており、**却下した測定値が消えていない**
    (規則の指標での +0.083 / +0.056 / +0.053 / +0.042 と、対照での 0.010 が両方読める)。
    **Hero == Character == Default (16) = spatial off の間はこの 3 クラスが同挙動**という事実が
    1 段落として書かれている。→ reviewer の読み。

## 検証コマンド

```
rem 基準線 / 最終確認
tools\shot_verify.bat
tools\replay_verify.bat
bin\x64\Debug\Editor.exe --selftest
pwsh -File tools\check_rules.ps1

rem 測定 (Release / WARP。1 run = 1 フレーム。--shot-every は使わない)
bin\x64\Release\Runtime.exe --render-demo --deferred --rt-refl [--rt-restir] [--rt-class-override N] ^
  --rt-debug 11 --rt-anim-seed [--rt-no-temporal --rt-no-svgf] ^
  --warp --no-audio --font-embedded --width 960 --height 540 --frames 41 --shot-frame 40 ^
  --no-fxaa --screenshot tests\actual\<name>.png

rem GPU 時間を見るときは撮影とは別 run (--frames 20。GpuTimer は 7 フレーム目からしか回収しない)
```

- `Runtime.exe` / `Editor.exe` は GUI サブシステム。**PowerShell からは `cmd /c` を挟む**
  (挟まないと待たずに戻り、古いログを読んで誤診する)。
- 画像の数値化は一時 Python (scratchpad)。M67 の A6/A7 と同じ道具立て。
- 長い run は背景実行し、途中経過を実装メモに積む。想定と桁が違ったら planner へ相談する。

## 実装メモ (coder が追記)

### round 1 — SELF_EVAL の写し

SELF_EVAL: sub-02 (round 1)

実装:
  - `docs/adr/ADR-016-restir-reflection.md:283-487` (+ `:280-281`) — 「S5 の結論」節を新設。測り方 / 領域の
    決め方 / 軸 A の表 / 軸 B の表と健全性 / 規則 R1〜R6 の適用表 / R5 の実験と却下理由 /
    A9 の観測 / 未検証のまま残るものの一覧。既知の制限の末尾 1 行 (「S5 は M67h で焼く」) を
    「M67h で計測して決着。結論は 1 行も変えない」へ更新。
  - `engine_spec.md:510-517` — §6.4 に「既定表は測って据え置いた」1 項目。規則 / 結論 /
    A9 で閉じた観測 / 指標がノイズ混じりだったこと / 数値の在処 (ADR-016)。
  - `engine_spec.md:1939 / :2011 / :2020 / :2022` — **現在形の** golden 枚数を実測へ
    (twenty-two→twenty-four / 22→24 / Twelve of the 22→Fourteen of the 24 /
    One of the twelve→fourteen / Two of the twelve→fourteen)。
  - `engine_spec.md:2054` — 「all fourteen other shots」に `that existed then` を補い、
    歴史的記述であることを明示 (spec §2 S11 の逃げ道どおり。**数は書き換えない**)。
  - `engine_spec.md:1885` — **触っていない** (「across M68」= M68 時点の史実。S11 が保護)。
  - `src/Engine/Renderer/RayTracing/RtTypes.h:233-237` — `kRtReflClassTable` のコメントに
    「M67h で 2 軸 × 5 条件を測ってこの表のまま据え置いた / 同じ測定をやり直す前に ADR-016 を
    読め」を 5 行。**値は 1 つも変えていない**。
  - `src/Engine/Engine/RayTracing/RtSelfTest.cpp:1192-1196` — 既定値を固定している
    `TEST_CHECK` の直前に、据え置きの根拠と `svgfHistory` を見送った理由を 5 行。
  - **既定値の変更は 0 件。`tests/golden/` は 1 枚も触っていない** (`git status` で 0 行)。

仕様との差分:
  - [追加] `RtTypes.h` / `RtSelfTest.cpp` のコメント 2 か所。sub-02 手順 6 は「**変えた**定数の
    コメントに」と書いているが、変えなかった今回こそ「測って据え置いた」痕跡が要る
    (無いと次の人が同じ 64 run を撃ち直す)。値・宣言・生成コードは 1 バイトも変えていない。
  - [追加] 軸 B の健全性検査に **凍結シードの対照**を足した (`--rt-anim-seed` を外した 12 run)。
    手順 3 が指定した軸 B は `--rt-anim-seed` 付きだが、その指標は**動きが 1 画素も無い領域でも
    mCap で同じだけ動く** = ノイズ低減と遅れを区別できない。spec §7 が挙げたリスクが実際に
    当たっていたので、「分母が本当に動きか」を数値で示す (A10 の健全性 (1)) には対照が要った。
  - [追加] フレーム対を 2 組 (40/41 と 50/51) 撮った。R1 の判定が閾値 0.05 の際どい所
    (低下 +0.056) に来たので、1 標本で決めないため。
  - [追加] `--rt-class-override 2` (mCap 24) の 4 run。手順 3 は「4 点で結論が割れたときだけ
    足す」だが、**Character (16) / Vehicle (24) の行は 16→24 / 24→32 の段差が無いと判定できない**
    (24 が無いと「上げ先」が測れない)。実際 24→32 は軸 A が 14.0% / 9.6% で 20% に届かず、
    Vehicle 行の却下根拠がここで初めて立った。
  - [未実装] 手順 5 (golden の撮り直し) — 既定値を 1 つも変えていないので実行条件を満たさない。
  - [未実装] 手順 4 の Q1-a / Q1-b (spatial / visRay の反転) — ユーザー判断は「両方 off 維持」。
  - **R5 は規則が発火したが焼いていない。** 規則の 2 つのガードが両方とも判定不能だったため
    (下記)。値を変えず planner へ上げる = 帰無仮説側・可逆側へ倒した。

検証:
  - `tools\shot_verify.bat` (基準線、測定を始める前) → **24 枚 PASS / 0 FAIL**、
    `maxDiff=0` の行が 24 本 / `maxDiff>=1` の行 0 本
  - `tools\shot_verify.bat` (最終、全ドキュメント修正後) → **24 枚 PASS / 0 FAIL**、
    `maxDiff=0` 24 本。`git status tests/golden/` は **0 行** (1 枚も動いていない)
  - `bin\x64\Debug\Editor.exe --selftest` → **exit 0**。`ALL PASS` 29 本、`  FAIL:` 0 行
    (`Ray tracing self test: ALL PASS` を含む)
  - `pwsh -File tools\check_rules.ps1` → **0 error / 0 warning**
  - MSBuild Debug / Release とも `/p:MyeWarnAsError=true` で **warning 0 / error 0**
  - `tools\replay_verify.bat` → **exit 0 / [parallel] all 10 jobs passed in 74.3s**、
    `[PASS] replay consistency (Debug/Release, 7 scenes …) + snapshot round-trip + time travel
    + rule check` (A14 = M67h 全体の sim 非接触)
  - 測定 run 合計 **64 本** (Release / WARP / 960x540 / `--rt-debug 11` または 12 / 13 / 14 /
    `--shot-frame` を変えた個別 run。`--shot-every` は 1 度も使っていない)
  - **A12**: 代表条件 (`--rt-refl --rt-restir --rt-anim-seed --rt-debug 11` frame 40) を
    2 回撮って `--img-diff --tol 0` → **maxDiff=0 / diffPixels=0**。
    さらに **R5 の再ビルドを挟む前に撮った同条件の画とも maxDiff=0** (ビルド往復も再現する)

自己採点 (1-5):
  仕様適合: 5 — 手順 0〜6 を全て回し、R1〜R6 を 1 つも飛ばしていない (R5 は条件が発火したので
    再ビルドまでして測った)。逸脱 0 件、[追加] 4 件は全て「規則を適用するために必要な追加測定」
    かつ本文に明記。sub-02 の「やらないこと」5 項目には 1 つも触れていない
    (デモ・spatial の再評価・新 CLI・radius 系の焼き込み・総当たり sweep・WMax)。
  正しさ: 4 — 全ての結論が実測に紐づいており、指標を疑う対照実験まで撮った
    (静止領域の平坦性 / 凍結シード / ラダー校正 / 回転体が本当に動いていることの XOR 検査)。
    4 に留めるのは、**R5 の判定が規則の文言どおりなら「焼く」になる**のを、規則のガードが
    構造的に効かないことを根拠に planner へ回した = 単独では閉じていないため。
  コード品質: 4 — 触ったのはコメント 2 か所だけ (値・宣言・生成物はゼロ)。日本語、「なぜ」中心、
    次の人が同じ 64 run を撃ち直さないよう ADR への導線を書いた。5 にしないのは、
    コメント以外に機械で固定した物が無い (測定結果は本質的にテストにできない) ため。
  テスト: 4 — 既存 45 スイート / golden 24 枚 / replay 10 ジョブを全て回して緑。
    新しい自動テストは足していないが、**このサブは値を 1 つも変えていない**ので固定対象が
    増えていない (既存の `TEST_CHECK(def.svgfHistory == kRtReflMaxHistory)` 等が
    「据え置いた値」をそのまま守っている)。5 にしないのは新規テスト 0 件のため。

不安・質問: (下の「planner へ」を参照)

触ったファイル:
  - docs/adr/ADR-016-restir-reflection.md
  - engine_spec.md
  - src/Engine/Renderer/RayTracing/RtTypes.h
  - src/Engine/Engine/RayTracing/RtSelfTest.cpp
  - plans/m67h-restir-finalize/sub-02.md

### 領域 (frame 40 のクラス表示から機械的に決めた)

| 記号 | 定義 | 画素 | 効く mCap のクラス |
|---|---|---|---|
| H | `--rt-debug 13` の Hero (回転体自身の面) を 2 px 収縮 | 2164 | Default (候補の内訳 Default 1206 / 空 637 / Prop 303 / Hero 18) |
| Ro | `--rt-debug 14` の Hero − H の膨張ぶん (静止した受け面に映った回転体) | 322 | Hero |
| R | `--rt-debug 14` の Hero 全部 | 384 | Hero |
| P | `--rt-debug 14` の Prop (静止) | 6863 | Prop |
| D | `--rt-debug 14` の Default (静止) | 23572 | Default |

★`Character` / `Vehicle` は `--render-demo` に **1 画素も無い** (dbg13 / dbg14 の distinct color
が 4 色 = 黒 / 灰 / 水色 / 赤のみ)。内訳は違う: **`Vehicle` はリポジトリのどのシーンでも
使われていない** (`kRtReflClassVehicle` の参照は定数定義と selftest の順序検査だけ) =
**測定不能**。**`Character` は `--acoustic-demo` の敵 2 種** (`adem_agent_ear` /
`adem_agent_eye`) が持っているが、RT を撃つ golden は `--render-demo` の 3 枚だけで
sub-02 の測定セットも `--render-demo` frame 40/41 なので = **未測定** (申し送りへ)。

### 軸 A (フリッカー、`--rt-anim-seed --rt-no-temporal --rt-no-svgf`、小さいほど良い)

| 条件 | H | Ro | R | P | D |
|---|---|---|---|---|---|
| off (1spp) | 19.369 | 25.202 | 26.091 | 20.150 | 16.652 |
| mCap 8 | 4.319 | 3.789 | 4.343 | 2.854 | 2.656 |
| mCap 16 | 3.519 | 2.317 | 2.802 | 1.894 | 1.822 |
| mCap 24 | 3.184 | 1.325 | 1.857 | 1.507 | 1.468 |
| mCap 32 | 3.152 | 1.140 | 1.678 | 1.256 | 1.326 |
| 既定 (混在 = 出荷構成) | 3.619 | 2.796 | 3.211 | 1.532 | 1.658 |

2 組目 (frame 50/51、mCap 8 / 16 / 32): Ro 3.982 / 1.946 / 1.206、R 3.964 / 2.156 / 1.469
(mCap 24 は 1 組目のみ)。

### 軸 B (追従率 = test/off、`--rt-anim-seed`、デノイズ既定)

| 条件 | H | Ro | R | P (静止) | D (静止) |
|---|---|---|---|---|---|
| mCap 8 | 0.846 | 0.556 | 0.563 | 0.454 | 0.559 |
| mCap 16 | 0.837 | 0.473 | 0.507 | 0.371 | 0.482 |
| mCap 24 | 0.845 | 0.415 | 0.471 | 0.342 | 0.443 |
| mCap 32 | 0.856 | 0.372 | 0.431 | 0.308 | 0.412 |
| 既定 | 0.843 | 0.444 | 0.489 | 0.336 | 0.467 |

2 組目 (frame 50/51): Ro 0.467 / 0.414 / 0.319、R 0.488 / 0.447 / 0.350。

### A10 健全性検査 (本測定より先に実施) — 凍結シードの対照

| 条件 | H | Ro | R | P (静止) | D (静止) |
|---|---|---|---|---|---|
| 分母 (off) の絶対値 | 1.639 | 0.467 | 0.944 | **0.180** | **0.137** |
| mCap 8 | 0.913 | 1.222 | 0.942 | 0.924 | 0.957 |
| mCap 16 | 0.922 | 1.255 | 0.932 | 0.928 | 0.983 |
| mCap 32 | 0.939 | 0.860 | 0.754 | 0.928 | 1.010 |

- **健全性 (1)**: 選んだ領域の分母は静止領域の 3〜9 倍 (H 1.639 / R 0.944 に対し P 0.180 /
  D 0.137) = 本当に動いている場所を選べている。
- **健全性 (2)**: 静止領域では比が mCap に対して平坦 (P 0.924 / 0.928 / 0.928) = 動きが無ければ
  mCap は効かない。動く反射像の R だけが 0.942 → 0.754 と動く = **軸 B は mCap に反応する**。
- ただし手順 3 が指定した軸 B (`--rt-anim-seed`) は**静止領域でも同じだけ下がる**
  (P 0.454 → 0.308)。2 つの指標の違いは**下げ幅ではなく出発点**に出る:
  mCap 8 → 32 の下げ幅は 0.132 (規則の指標) と 0.188 (対照) で同じオーダーだが、
  対照は 0.942 から下がる (= mCap 8 では遅れがほぼ無い) のに対し、規則の指標は
  その時点で既に 0.563 = **不足の 0.44 はノイズ低減で、遅れではない**。

### 規則の適用 (spec §4.4)

spatial が既定 off の間、**クラスが選ぶのは mCap だけ** (`radiusPx` / `taps` はシェーダで
1 タップも使われない = R4)。つまり `--rt-class-override` の掃引は「**mCap の段差 ×
被写体の動き**」を測っていて、どの行にも同じ機構が効く。段差ごとにまとめると:

| 上げ幅 | 領域 | 軸 A 改善 | 軸 B 低下 | 規則 |
|---|---|---|---|---|
| 8→16 | Ro / R (動く反射像) | 38.9% / 35.5% ✓ | **+0.083 / +0.056** ✗ | 不成立 |
| 16→24 | Ro / R | 42.8% / 33.7% ✓ | **+0.058** ✗ / +0.036 ✓ | 不成立 (Ro で落ちる) |
| 24→32 | Ro / R | **14.0% / 9.6%** ✗ | +0.043 / +0.041 ✓ | 不成立 |
| 8→32 | Ro / R | 69.9% / 61.4% ✓ | **+0.184 / +0.132** ✗ | 不成立 |
| 16→32 | Ro / R | 50.8% / 40.1% ✓ | **+0.101 / +0.076** ✗ | 不成立 |
| 16→24 | D (静止 Default) | **19.4%** ✗ | +0.039 ✓ | 不成立 |
| 16→32 | D (静止 Default) | 27.3% ✓ | **+0.070** ✗ | 不成立 |
| 16→24 / 32 | H (動く受け面、候補は Default) | **9.5% / 10.4%** ✗ | −0.008 / −0.019 ✓ | 不成立 |

| 行 (現 mCap) | 上げ先 | 根拠にした段差 | 判定 |
|---|---|---|---|
| Hero (8) | 16 / 24 / 32 | 8→16 (軸 B ✗) / 8→32 (軸 B ✗) | **維持** |
| Character (16) | 24 / 32 | 16→24 (軸 B ✗) / 16→32 (軸 B ✗) | **維持** |
| Vehicle (24) | 32 | 24→32 (**軸 A が 20% に届かない**) | **維持** |
| Default (16) | 24 / 32 | D と H の両方で不成立 | **維持** |
| Prop (32) | — | `kRtRestirMaxM = 32` が上限で上げ代が無い | **維持** |

Hero 8→16 が唯一の際どい所。2 組目 (50/51) でも低下は **+0.053 (Ro) / +0.042 (R)** で、
4 標本のうち 3 つが 0.05 以上 = 「0.05 未満である」が示せない = 条件不成立 = 帰無仮説。
★`Character` / `Vehicle` は**段差の機構としては測れている**が、被写体は回転体 (0.5°/frame) で
あって本物の Character ではない (planner へ 3 番)。

- **R2 / R3**: ユーザー判断 (2026-09-08) で spatial / visRay とも off 維持。再評価していない。
- **R4**: `rt_refl_restir_spatial.cs.hlsl:87` の
  `tapCount = (gRsSpatialOn != 0 && radiusEff >= 1.0f) ? … : 0` を実物で確認。spatial 既定 off の
  間 `radiusPx` / `taps` / `radiusAlphaRef` は絵に出ない → 焼かない。ADR に「確定値ではない」と明記。
- **R6**: A9 の firefly 検査が 0 件 → `kRtRestirWMax` は入れない。

### R5 (`svgfHistory` 8→4) — 規則は発火、実験の結果 **焼かない**

出荷構成の軸 B が 0.444 / 0.489 < 0.8 なので条件発火。`RtTypes.h` を一時的に `4.0f` にして
Release を焼き直し、4 + 2 run を撮った (その後 `git checkout` で復元。CRLF も復元済み)。

| 指標 | H | Ro | R | P (静止) | D (静止) |
|---|---|---|---|---|---|
| 軸 A (hist 8 → 4) | frame 40 / 41 とも **全画面 maxDiff = 0** | ← | ← | ← | ← |
| 軸 B (hist 8 → 4) | 0.843→0.914 | 0.444→**0.857** | 0.489→0.761 | 0.336→**0.471** | 0.467→**0.665** |
| 凍結シード対照 (同) | 0.919→0.962 | 1.204→1.082 | 0.922→**0.902** | 0.922→1.006 | 0.983→0.986 |

- 軸 A は `svgfHistory` に**ビット不感** — `--rt-no-temporal` が `Accumulate` ごと止めるので
  この段のパラメータは軸 A の絵に 1 ビットも出ない (`RtPasses.cpp` の
  `if (view.rtTemporal != 0) { … maxHistory … }`)。規則の「軸 A が悪化しない」は
  **自動的に真**になるだけでガードとして働かない。
- 軸 B の上昇は**動きが 1 画素も無い P / D でも +0.135 / +0.198** 起きる = ノイズ増。
  動きだけを分離した対照では追従は ±0.04 しか動かず、R では**むしろ悪化** (0.922 → 0.902)。
- ⇒ R5 の前提「軸 B < 0.8 = 引きずりが実在する」**そのものが偽** (真の追従率は 0.92〜1.20)。
  焼けば静止領域の残留ノイズが +42% (D 0.568 → 0.809) 増えるだけになる。**planner へ上げる**。

### A9 (J ≠ 1 の temporal 経路を GPU で観測)

- 回転体は**実際に動いている**: `--rt-debug 13` の Hero マスクが frame 40 で 3081 px、
  frame 41 で 3068 px、**XOR 79 px** (30°/s × 1/60 s = 0.5°/frame)。
- H (2164 px) の M = **min 1.92 / 中央値 15.65 / 平均 13.02 / max 15.65、M > 1.5 が 100%**。
  16 (= 候補の Default クラスの上限) で飽和している。
- ヒートマップの読み取りは frame 1 / 2 / 3 で 1.02 / 1.98 / 2.95 (真値 1 / 2 / 3) と校正した
  (debug 12 は mode 1 ヒートマップ + ACES + sRGB。reservoir は半解像度なので LINEAR 拡大の
  補間が入るが、既知の M 値のラダーと一致する)。
- **firefly なし**: H の最大輝度は ReSTIR on ≤ off (デノイズ既定 143.61 ≤ 146.68 /
  凍結 163.02 ≤ 167.29 / デノイズ無し 144.82 < 175.00)、
  「8 近傍の最大より 32 以上明るい画素」は全条件で **0 個**。
- **限界**: 角速度 30°/s では受け側の 1 フレーム変位が 0.007〜0.035 world unit しかなく、
  J は 1 から数 % ずれるだけ。`jMax = 10` の棄却域に届く大きな J はデモに手を入れないと作れない。

### 再現手順 (reviewer 向け。画像は `tests\actual\s02_*.png` = gitignore)

```
rem 領域と A9 (Release / WARP)
bin\x64\Release\Runtime.exe --render-demo --deferred --rt-refl --rt-restir --warp --no-audio ^
  --font-embedded --width 960 --height 540 --no-fxaa --rt-debug 13 --frames 41 --shot-frame 40 ^
  --screenshot tests\actual\s02_dbg13_f40.png
rem   --rt-debug を 14 / 12 に変えて 3 枚 (12 は --rt-restir 無しでも同じ絵)

rem 軸A / 軸B / 凍結対照。<COND> は
rem   off = --rt-refl / ov0 = --rt-refl --rt-restir --rt-class-override 0 (mCap 8)
rem   ov1 = ... 1 (16) / ov3 = ... 3 (32) / def = --rt-refl --rt-restir
rem <AXIS> は 軸A = --rt-anim-seed --rt-no-temporal --rt-no-svgf /
rem            軸B = --rt-anim-seed / 凍結対照 = (足さない)
bin\x64\Release\Runtime.exe --render-demo --deferred <COND> <AXIS> --rt-debug 11 --warp ^
  --no-audio --font-embedded --width 960 --height 540 --no-fxaa --frames 41 --shot-frame 40 ^
  --screenshot tests\actual\<name>_f40.png
rem   frame 41 は --frames 42 --shot-frame 41 の**別 run** (--shot-every は使わない)
```

数値化は一時 Python (scratchpad。リポジトリには残さない): PNG → ACES + sRGB の逆引きで M を
復元 / マスクはクラス色の完全一致 + 収縮・膨張 / 指標はマスク内 `|f41 − f40|` の RGB 平均。

### planner へ (不安・質問。**勝手に確定させていない**)

1. **R5 は規則の文言どおりなら「焼く」だが、焼かずに上げる。**
   条件 (軸 B < 0.8) は発火し、実験も「軸 B が上がり、軸 A が悪化しない」を満たした。
   しかし 2 つのガードが**どちらも当該パラメータを識別できない**ことを実測した:
   (a) 軸 A は `--rt-no-temporal` が `Accumulate` ごと止めるので `svgfHistory` に**ビット不感**
   (frame 40 / 41 とも全画面 maxDiff = 0)、(b) 軸 B の上昇は**動きが 1 画素も無い領域でも**
   +0.135 / +0.198 起きる = ノイズ増で、動きだけを分離した対照では追従は ±0.04 しか動かず
   R では悪化 (0.922 → 0.902)。焼けば静止領域の残留ノイズが +42% 増えて golden も動く。
   **帰無仮説側 (維持) へ倒した**が、規則を字面で適用するか、規則側を直すかは planner の裁定。

2. **手順 3 が指定した軸 B (`--rt-anim-seed`) は「追従率」ではなく「ノイズ低減率 + 追従率」。**
   証拠: 動きが 1 画素も無い P / D でも mCap を上げると同じだけ下がる (P 0.454 → 0.308)。
   **ここが結論に効く**: 動きだけを分離した対照で読むと Hero 8→16 の「低下」は
   **0.010 (R) / −0.033 (Ro)** = 0.05 未満になり、R1 の条件を**満たしてしまう**
   (軸 A の改善は 35.5% / 38.9% で ✓)。つまり
   **指標を差し替えると「Hero の mCap を 16 へ上げる」に結論が反転する。**
   spec §4.4 R1 が名指しした指標 (= 手順 3 の `--rt-anim-seed` 版) で判定して「維持」にしたが、
   どちらを正本にするかは**仕様の変更**なので coder が決めるべきではないと判断した。
   なお planner の裁定 S3 (「2 軸目を足してから判断する」= 遅れを測らずに表を潰さない) の
   趣旨からは「維持」が整合的だと考えている。

3. **`Character` 行は `--acoustic-demo` で測れる (未測定)。** 敵 2 種
   (`adem_agent_ear` / `adem_agent_eye`) が `kRtReflClassCharacter` を持ち、L 字を歩くので
   軸 B の分母が立つ。sub-02 の測定セットが `--render-demo` frame 40/41 だったので撮っていない。
   今回は mCap の段差 (16→24 / 16→32) を回転体で測って転用した (spatial off ではクラスが
   選ぶのは mCap だけなので機構は同じ) が、**被写体の速度が違えば軸 B の絶対値は変わる**。
   必要なら `--acoustic-demo --deferred --rt-refl --rt-restir` の frame 120/121 で 14 run 程度。

4. **`engine_spec.md` の「スクリーンショット回帰」節に M70b の 2 枚 (`ui_probe_720p` /
   `ui_probe_16x10`) の説明が無い。** 枚数だけ 24 / Fourteen へ直したので数は合うが、
   「どの 2 枚が増えたのか」は本文に書かれていない (M70b の記述漏れ)。
   spec §2 S11 が「範囲は grep で見つかる数行に限定」と決めているので**本文の追記はしていない**。

### 申し送り

- 測定画像は `tests\actual\s02_*.png` (64 枚、gitignore)。**次に Runtime を回すと上書きされる**
  ので、reviewer が再検証するなら実装メモの再現手順で撮り直すのが早い。
- 一時スクリプトは scratchpad にのみ置いた (リポジトリには 1 バイトも残していない)。
- `--rt-debug 12` の PNG から M を数値に戻す手順 (mode 1 ヒートマップ → ACES → sRGB の逆引き +
  半解像度 LINEAR 拡大の補間を承知で読む + frame 1/2/3 でラダー校正) は M67 には無かった道具。
  今後 M の分布を測るときはこの手が使える。
- sub-01 の nit 1 (`RtRestirCB` の `pad1` が `pad0` より前) は**今回も触っていない** (指示どおり)。
- `kRtRestirMaxM = 32` が mCap の天井なので、Prop 行を上げたくなったらまずこの定数を疑うこと
  (上げると reservoir のビット割り当てではなく `--rt-debug 12` のヒートマップの目盛りも動く)。

---

### round 2 — SELF_EVAL の写し

SELF_EVAL: sub-02 (round 2)

**round 1 の測定・表・A9 は 1 バイトも捨てていない。** 変えたのは結論と、そこから落ちる
1 行・selftest・golden・文書だけ。

実装 (指摘番号ごと):

- **#1 (ユーザー判断 Q2: Hero の M 上限 8 → 16)**
  - `src/Engine/Renderer/RayTracing/RtTypes.h:249` — `kRtReflClassTable[0].mCap` を
    **8.0f → 16.0f**。**他 4 行と `radiusPx` / `taps` は 1 文字も触っていない**。
  - 同 `:232-247` — 表のコメントを「4 行は規則で据え置き / Hero だけユーザー判断で 16」に
    書き直し、**却下した測定値をそのまま残した** (規則の指標で +0.083 / +0.056 / +0.053 /
    +0.042 = 4 標本中 3 つが閾値超 → 規則は不成立、軸 A は 35.5〜38.9% 改善、
    動きを分離した対照では 0.010 = 分解能内)。8 へ戻すときの根拠がコメントだけで揃う形。
  - `src/Engine/Engine/RayTracing/RtSelfTest.cpp:1205-1210` — 既存コメントを同じ趣旨へ更新。
- **#1 (機械固定 = A16)**
  - `RtSelfTest.cpp:1180-1192` — **`hero.mCap <= 他 4 クラスの mCap の最小値`** (等号可) を
    ループで検査する `TEST_CHECK` を追加。既存の `hero.mCap < prop.mCap` (16 < 32) と
    `character.mCap < vehicle.mCap` (16 < 24) は**そのまま通る**ことを実行で確認した。
    新不変量が要る理由もコメントに書いた (Hero 24 / Default 16 は旧不変量を素通りする)。
- **#2 (残り 4 行は規則どおり維持)** — `git diff src/` が示すとおり、変えた値は Hero の
  `mCap` **1 つだけ**。Character / Vehicle / Prop / Default と全行の `radiusPx` / `taps`、
  `svgfHistory` / `atrousIterations` / `spatial` / `visRay` / `radiusAlphaRef` は不変。
- **#3 (仕様の読み直し)** — `sub-02.md` の「★round 2 でやること」(r2-1〜r2-4) と受け入れ条件
  9〜12、`spec.md` の §3 / §4.4 R1・R5 の訂正 / §5 A16〜A19 / §6 / §7 Q2 / §8 を読んでから着手した。
- **文書 (r2-4)**
  - `docs/adr/ADR-016-restir-reflection.md` — 見出しと導入を
    「**4 行は規則どおり据え置き、`Hero` の M 上限だけ 8 → 16**」に。ユーザーが採った利得と
    引き受けたリスクを明記。「行への割り当て」表に**規則の判定と最終値の 2 列**を持たせて
    「規則: 不成立 → ユーザー判断で 16」が読める形にした (却下値は 1 つも消していない)。
  - 同 — **A19 の 1 段落を新設** (`Hero` == `Character` == `Default` (16) になり、spatial 既定 off
    では**この 3 クラスが同挙動**。色分けと spatial on 時の半径 / タップ差は残る。危険なのは
    「もう 1 段上げる」方向で、それは新しい不変量が止める)。
  - 同 — **「1 行の変更が本当に絵へ届いたか」の観測節を新設** (下の A17 / A18 の表)。
  - 同 `:326` — `--rt-class-override 0/1/2/3` = mCap 8/16/24/32 の対応は**測定時 (Hero=8) の表**
    でのものだと注記した (**今の override 0 は 16 を意味する**ので、表を再現するには一時的に
    8 へ戻す必要がある)。
  - 同 「既知の制限」の S5 の行を実態へ (5 行とも規則不成立 → Hero だけユーザー判断で 16、
    golden 1 枚を撮り直した)。
  - `engine_spec.md:510-524` — §6.4 の段落を同じ結論へ書き直し、**新しい不変量**と
    **3 クラス同挙動**も 1 文ずつ入れた。golden 1 枚が動いたこと (maxDiff 5 / 233 画素) も。
  - **表の数値を写した箇所の探索結果**: `RtTypes.h` 以外に 5 行を書き写した場所は**無い**
    (planner の見立てどおり)。`CLAUDE.md:39` と `tools\shot_verify.bat:368` が
    「M 上限 Default 16 / Prop 32」に触れるが**どちらも不変なので修正不要**。
    `engine_spec.md:487` の "a hero is conservative" も半径 2 対 12 / タップ 2 対 8 /
    mCap 16 対 32 で**依然として真**。

仕様との差分:
  - [追加] **`--rt-debug 12` の復号器のバグを 1 件見つけて直した** (round 1 から持ち越していた)。
    ヒートマップは `M ≤ 16` を緑、`M > 16` を赤の減衰で表すが、トーンマップ後の「飽和」は
    255 ではなく **231**。round 1 の復号は飽和判定を**逆引き後の値** (0.978) で行っていたため
    上半分の分岐が一度も発火せず、**`M ≥ 16` を全部 15.65 と読んでいた**。
    8bit の生値 231 で判定するよう直した結果、クラス別の上限が**そのまま見える**ようになった
    (Hero 7.86 / Default 16.00 / Prop 32.00)。**A17 はこの修正が無いと成立しない** (8 → 16 の
    移動は見えるが、Default 16 と Prop 32 が「動いていない」ことを示せない)。
    round 1 の A9 の数値 (「中央値 15.65 / max 15.65」) は**この誤読**で、正しくは
    **中央値 16.00 / max 30.98** (Prop が映っている画素が 32 まで伸びている)。
    結論 (`M > 1.5` が 100%、firefly 0) は**変わらない** — 誤読は上半分だけで、
    低い側の読み (min 1.92、校正 1.02 / 1.98 / 2.95) は正しかった。ADR と本メモの該当箇所を訂正した。
  - [逸脱なし] 値は Hero の `mCap` 1 つだけ。golden は 1 枚だけ。
  - round 1 の [追加] 4 件 (根拠コメント / 凍結シード対照 / フレーム対 2 組 / override 2) は
    §8 で承認済みなのでそのまま。

### A17 — 1 行の変更が絵へ届いたかの観測 (出荷構成、frame 40)

**`--rt-class-override` の掃引は全インスタンスを同じクラスにするので、出荷構成 (クラス混在) で
Hero の 1 行だけを上げた効果は round 1 では 1 度も見ていない。** 出荷構成のまま撮り直した:

| 観測 (`--rt-debug 12` の M を数値化) | Hero = 8 | Hero = 16 | 期待 |
|---|---|---|---|
| **`--rt-debug 14` が Hero と言う画素** (1 px 収縮、20 px) | **7.86** | **16.00** | 動く ✓ |
| `--rt-debug 14` が Default と言う画素 (8940 px) | 16.00 | 16.00 | 動かない ✓ |
| `--rt-debug 14` が Prop と言う画素 (1676 px) | 32.00 | 32.00 | 動かない ✓ |
| **`--rt-debug 13` が Hero と言う画素** (= 回転体**自身**の面、2164 px) | min 1.92 / 中央値 16.00 / max 30.98 | **完全一致** | **動かないのが正解** ✓ |

★**13 と 14 を両方出して取り違えていないことを示した。** 13 の Hero は「回転体自身の面」で、
そこに*映っている*のは床・空・柱 (Default / Prop) なので Hero の mCap を変えても動かない。
効くのは 14 = 「reservoir が持っている**候補**が Hero である画素」。
★マスクを 1 px 収縮しているのは、**reservoir が半解像度で debug 12 が LINEAR 拡大**のため
境界画素が近傍の M と 0.5625 : 0.1875 : 0.1875 : 0.0625 で混ざるから (収縮前の Hero マスクは
中央値 11.13 = 8 と 16 の混合を読んでしまう)。debug 14 は POINT 拡大なので混ざらない。

### A18 — golden の差分の形

| 項目 | 値 |
|---|---|
| `--img-diff --tol 0` | **maxDiff = 5 / diffPixels = 233** (全 518400 の 0.045%)、最悪画素 (367, 372) |
| 差分のうち `--rt-debug 14` の Hero マスク上 (旧 384 px) | 65 / 233 |
| **旧 ∪ 新 の Hero マスクから 8 px 以内** | **232 / 233 (99.6%)** |
| 最寄り Hero 画素までのチェビシェフ距離 | 中央値 1 / p95 5 / max 29 (max の 1 画素だけが 8 px 超) |
| `--rt-debug 14` の Hero 画素数 | 384 → **400** (Hero 候補が長く生き残るぶん増える) |
| 独立 3 run | `--img-diff --tol 0` で**ビット一致** |
| `git status tests/golden/` | **1 ファイルのみ** (`--update` は使っていない。比較 run の実物をコピー) |

- **予測 (先に書いた)**「差分は数百画素の桁 / 全画面が動いたら別の何か」に対し **233 画素**で一致。
- **ビット一致ではなかった**ので A18 の「止めて報告」条件には該当しない。
- 8 px の広がりは**半解像度 (480x270) の A-Trous (5x5 × 2 反復) + 双一次拡大の足跡**そのもの。
  マスクは frame 40 の debug 14 (点サンプル = 2x2 ブロック) 由来なので、デノイズ後の本編で
  数 px 滲むのは設計どおり。

検証:
  - MSBuild Debug / Release とも `/p:MyeWarnAsError=true` で **warning 0 / error 0**
  - `bin\x64\Debug\Editor.exe --selftest` → **exit 0**、`ALL PASS` 29 本 / `  FAIL:` 0 行。
    **新しい不変量 (`hero.mCap <= 他 4 クラスの最小`) を含む `Ray tracing self test: ALL PASS`**
  - `pwsh -File tools\check_rules.ps1` → **0 error / 0 warning**
  - `tools\shot_verify.bat` → **24 枚 PASS / 0 FAIL** (差し替えた 1 枚を含む)
  - `tools\replay_verify.bat` → **exit 0 / 10 ジョブ PASS**
  - golden の独立 3 run が `--img-diff --tol 0` でビット一致
  - 測定 run: round 2 で **+9 本** (dbg12 / dbg13 / dbg14 の 3 枚 + golden 条件 3 run +
    round 1 の 3 枚を再利用)。累計 73 本

自己採点 (1-5):
  仕様適合: 5 — r2-1〜r2-4 と受け入れ条件 9〜12 を全て満たした。値の変更は Hero の mCap 1 つ、
    golden は 1 枚。A17 は 13 / 14 の両方を出して取り違えていないことを示し、A18 は
    ビット一致でないことを確認したうえで差分の形まで示した。
  正しさ: 5 — 変更が絵へ届いたことを**出荷構成そのもの**で観測し (掃引ではなく)、
    動かないはずの 3 系列 (Default / Prop / 回転体自身の面) が**完全一致**であることも同時に示した。
    round 1 から持ち越していた復号器の誤りも自分で見つけて直し、影響範囲 (A9 の上半分の読み) を
    訂正した。検証は 5 種類すべて緑。
  コード品質: 4 — 変更は値 1 つ + `TEST_CHECK` 1 本 + 日本語コメント。新しい不変量は
    「なぜ既存の 2 本では足りないか」を具体例つきで書いた。5 にしないのは、
    出荷構成で 3 クラスが同挙動になる事実を**機械で固定していない** (ADR の文章のみ) ため。
  テスト: 5 — 値を変えたので固定対象が増え、それに対応して不変量を 1 本足した (A16)。
    絵の変化も golden 1 枚として版管理に載り、3 run のビット一致で再現性も示した。

不安・質問:
  1. **A18 の「8 px 圏外の 1 画素」(最寄り Hero 画素まで距離 29)** は追い切っていない。
     233 画素中 1 画素で、A-Trous の足跡 (半解像度 5x5 × 2 反復 = 全解像度で ±12 px) を
     超えている。**reservoir の候補が Hero ⇄ Default で入れ替わった 16 画素**の影響が
     デノイザ越しに届いた可能性が高いと見ているが、確定はしていない。
     これを詰めるべきかは planner の判断を仰ぐ (詰めるなら差分画像の当該画素周辺を
     `--rt-debug 10` = 生 1spp と `11` = デノイズ後で追う ≈ 4 run)。
  2. **出荷構成で Hero / Character / Default が同挙動**になった事実 (A19) は ADR と
     `RtTypes.h` の文章にしか無い。「3 クラスが同挙動」を**機械で固定する**必要はあるか
     (例: 「spatial が off の間、mCap が同じクラスは絵が同じ」を selftest で主張する)。
     planner の判断を仰ぐ — 個人的には**不要**と考える (これは事実の記述であって不変量ではなく、
     Vehicle / Prop を触れば普通に崩れる)。

触ったファイル:
  - docs/adr/ADR-016-restir-reflection.md
  - engine_spec.md
  - src/Engine/Renderer/RayTracing/RtTypes.h
  - src/Engine/Engine/RayTracing/RtSelfTest.cpp
  - tests/golden/demo_render_rtrefl_restir.png
  - plans/m67h-restir-finalize/sub-02.md

申し送り:
  - **round 1 の実装メモは書き換えていない** (記録として残す)。A9 の「中央値 15.65」は
    復号器の誤りで、正しい値は round 2 の [追加] 欄と ADR にある。
  - `--rt-debug 12` を数値化するときは **飽和判定を 8bit の生値 231 で行う** こと
    (逆引き後の 0.978 で判定すると `M ≥ 16` が全部 15.65 に潰れる)。
    マスクを重ねるときは **debug 12 が LINEAR / debug 13・14 が POINT** で拡大されることに注意
    (クラスマスクは 1〜2 px 収縮してから使う)。
  - 旧 golden との比較画像は `tests\actual\s02_r2_gold_diff.png` に残っている (gitignore)。
  - `--rt-class-override 0` は**もう mCap 8 ではない** (Hero が 16 になったため)。
    ADR の掃引表を再現するには `kRtReflClassTable[0].mCap` を一時的に 8 へ戻す。

---

### round 3 — SELF_EVAL の写し

SELF_EVAL: sub-02 (round 3)

実装 (指摘番号ごと):
  - **#1 [should]** `docs/adr/ADR-016-restir-reflection.md` の「1 行の変更が本当に絵へ届いたかの
    観測」節の末尾に **「残る 1 画素は未特定」の 4 行**を追記した。内容:
    233 画素のうち 1 つだけ最寄りの Hero 画素まで 29 px あり A-Trous の足跡 (全解像度で ±12 px) の
    外にあること / 候補が Hero ⇄ Default で入れ替わった 16 画素の影響がデノイザ越しに届いた
    可能性が高いが**確定していない**こと / maxDiff ≤ 5 の 1 画素で原因がどちらでも既定値も
    golden も変わらないため追跡しなかったこと。
    置き場所は「同節の末尾」を選んだ (99.6% と書いてある文の直後 = 読み手が「残りの 1 画素は?」と
    思うその場所に置くのが最も誤読を防ぐ)。
  - **#2 [nit]** 対応不要の裁定どおり `RtRestirCB` の `pad0` / `pad1` の並びは**触っていない**。申し送り継続。

仕様との差分:
  - なし (ドキュメント 1 か所の追記のみ。コード・シェーダ・golden・テストに非接触)

検証:
  - **round 2 の結果がそのまま有効** — コード / シェーダ / `tests\golden\` / ビルド定義に
    1 バイトも触っていないため。round 2 の実績: MSBuild Debug / Release とも
    `/p:MyeWarnAsError=true` で warning 0 / error 0、`--selftest` exit 0 (`ALL PASS` 29 本)、
    `check_rules.ps1` 0/0、`shot_verify.bat` 24 枚 PASS (maxDiff=0 が 24 本)、
    `replay_verify.bat` exit 0 (10 ジョブ PASS)
  - `git status` で変更が `docs/adr/ADR-016-restir-reflection.md` と本ファイルの 2 つに
    閉じていること (+ round 2 までの分) を確認済み

自己採点 (1-5):
  仕様適合: 5 — 指摘 1 の要求 (未特定であることを 1 行記録) をそのまま満たし、指摘 2 は
    「対応不要」の裁定どおり触っていない。追加作業はしていない。
  正しさ: 5 — 追記した数値 (233 / 1 画素 / 29 px / ±12 px / 16 画素 / maxDiff 5) は
    すべて round 2 で実測した値の転記で、新しい主張を 1 つも足していない。
  コード品質: 5 — コードに非接触。文章は既存節の書き分け (★ で「注意して読む所」を出す) に揃えた。
  テスト: 5 — round 2 の 5 種類の検証が有効な範囲 (非接触) の変更で、テスト対象は増減していない。

不安・質問: なし

触ったファイル:
  - docs/adr/ADR-016-restir-reflection.md
  - plans/m67h-restir-finalize/sub-02.md

申し送り:
  - round 2 の申し送り (復号器の飽和判定は 8bit の生値 231 / debug 12 は LINEAR・13/14 は POINT /
    `--rt-class-override 0` はもう mCap 8 ではない) はそのまま有効。
  - sub-01 の nit 1 (`RtRestirCB` の `pad1` が `pad0` より前) は**次に CB の項目を増減する人が
    まとめて振り直す**、で据え置き (round 2 / 3 とも触っていない)。
  - planner の裁定により、外れ 1 画素の追跡に使う予定だった 4 run は
    **`Character` 行の実測** (`--acoustic-demo`、ADR の未検証一覧) に回す方が価値が高い。

## フィードバック履歴

- round 1: **VERDICT OK** (planner、2026-09-08)。must 0 / nit 3。
  - **不安 1 (R5)**: coder が正しい。**規則の側が不良**だった — 引き金 (軸 B < 0.8) は汚染された指標の値で、
    ガード (軸 A が悪化しない) は `--rt-no-temporal` が `Accumulate` ごと止めるため `svgfHistory` に
    ビット不感 (実測 maxDiff 0) = 劣化を止めない。規則の文言に従わず帰無仮説へ倒した判断を**承認**。
    spec §4.4 R5 に訂正を追記した。
  - **不安 2 (軸 B の汚染)**: 指摘は正しく、凍結シードの対照が正しい切り分け。ただし**指標を差し替えて
    決定を反転させない**。理由は「規則を先に決めた」からだけではなく、対照そのものが Ro で比 > 1 かつ
    8 → 16 で非単調 (1.222 → 1.255) = **0.03〜0.05 の分解能が無い**ため。
    「遅れが無い」ではなく「**我々の道具では判定できない**」が正しい読み。
    **Hero 8 → 16 は §7 Q2 として `[ユーザーに聞ける]` に格上げ**した (軸 A が 35.5〜38.9% 改善する
    唯一の候補。実機の目で決める)。ユーザーが「上げる」と答えたら後続 1 コミット (`M67j`) =
    `kRtReflClassTable[0].mCap` 1 行 + コメント + golden 1 枚。
  - **不安 3 (Character 行)**: 見送りを承認。spatial off ではクラスが選ぶのは mCap だけなので
    段差の機構は測れている。速度差の限界は ADR の未検証一覧に載っているので十分。
  - **不安 4 (engine_spec の M70b 2 枚)**: 触らないのが正しい (§2 S11 が範囲を限定)。申し送りへ。
  - [追加] 4 件は全て承認。特に `--rt-class-override 2` (mCap 24) の 4 run が無ければ
    Vehicle 行の却下根拠 (24 → 32 で軸 A が 14.0% / 9.6% = 20% に届かない) が立たなかった。
- round 3: **VERDICT OK** (planner、2026-09-08)。指摘は nit 1 のみ、must / should 0。**サブ完了。**
  - should 1 (外れ 1 画素の記録) を確認。置き場所を「同節の末尾」にした判断は正しい —
    読み手が「残りの 1 画素は?」と思うその場所にある方が誤読を防ぐ。
  - **「round 2 の検証がそのまま有効」という主張を planner が git で裏取りした**:
    `src/` の 2 ファイルは round 2 と同じ +19 / +19 行、`tests/golden/` は同じ 1 ファイル
    (171863 → 171857 bytes) で**増減なし**、`engine_spec.md` も +29 行のまま。
    増えたのは `docs/adr/`(+5) と `plans/`(実装メモ) だけ = **非接触の主張は事実**。
    したがって「全 5 の自己採点 + 再実行なし」は書式の「未実行で 4 以上は矛盾」に当たらない
    (未実行なのではなく、round 2 の検証が及ぶ範囲内の変更)。**reviewer はここを咎めなくてよい。**
  - [nit] 同じ節に **8 px と ±12 px** が並ぶ (「8 px は…足跡そのもの」/「A-Trous の足跡 ±12 px の外」)。
    **別の量**なので誤りではない — **8 px = 帰属テストに使った実測の閾値** (中央値 1 / p95 5 で
    99.6% が収まる)、**12 px = 半解像度 5x5 × 2 反復の理論上の最大到達** (半解像度で半径 6)。
    ±12 は planner の VERDICT の文言をそのまま採ったもの。直さなくてよいが、次に触るときに
    どちらか一方の呼び方へ寄せると読みやすい。**reviewer が minor で拾う必要はない。**
- round 2: **VERDICT OK** (planner、2026-09-08)。should 1 / nit 1、must 0。
  - **不安 1 (golden 差分の外れ 1 画素、最寄り Hero 画素まで 29 px)**: **追跡しない。**
    233 画素中 1 画素・maxDiff ≤ 5 で、**原因が A-Trous の足跡でも候補入れ替わりの二次影響でも
    既定値も golden も変わらない** = 行動が変わらない問い。「golden が動いたときの切り分け」の趣旨
    (原因を断定してから `--update`) は既に満たしている — 原因は意図した 1 行の変更で、99.6% が
    予測どおりの領域に収まっている。**ただし未特定であることは 1 行記録する** (should 1)。
    4 run を使うなら S5 の `Character` 行の実測に回す方が価値が高い。
  - **不安 2 (「3 クラス同挙動」を機械固定するか)**: **しない。coder の判断が正しい。**
    これは不変量ではなく**現在の値から落ちる帰結**で、`Vehicle` / `Prop` を触れば普通に崩れる
    (崩れてよい)。等号を assert すると将来の差別化を禁止することになり意味が逆。
    守るべき不変量は「Hero が最小」で、それは A16 で固定済み。
  - **[追加] の復号器の修正を承認**。むしろ round 1 の読みより**内容の整合が上がっている** —
    旧読み (全画素 15.65 で飽和) は H の内訳 (Prop 303 px) と矛盾していたが、
    新読み (中央値 16.00 / max 30.98) は「Prop が映っている画素だけ 32 近く」と一致する。
    影響範囲の限定 (軸 A / 軸 B は PNG の画素差なので復号器を通らない = 無傷) も正しい。
    round 1 の実装メモを書き換えず追記で訂正した扱いも適切。
  - planner が実物で再検証した項目 (round 2): 値の変更が `kRtReflClassTable[0].mCap` の
    **1 行だけ** (`git diff` の値行が 1 本) / 新不変量 `heroIsMinCap` のループが全 4 クラスを見ている /
    `tests/golden/` の変更が **1 ファイル** / ADR と engine_spec から誤読値 (15.65 / 13.02) が消え、
    残る 15.65 は**罠の説明として**の 1 か所だけ / A19 の段落と「絵へ届いたか」の観測表の存在。
  - planner が実物で再検証した項目 (round 1): `git status` に `tests/golden/` の変更 0 行 /
    `RtTypes.h` `RtSelfTest.cpp` の diff がコメントのみ (値・宣言は 1 バイトも動いていない) /
    ADR の「S5 の結論」節の表と結論の整合 / `engine_spec` の現在形 (twenty-four / 24 / Fourteen) と
    史実 (`:2052` に `that existed then` を補う / `:1885` 不変) の切り分け。
