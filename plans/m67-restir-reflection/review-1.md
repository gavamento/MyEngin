# review-1 — M67 ReSTIR 反射 + ReflectionClass

- 対象: 30f4993..HEAD (73a439e M67a / 4511cce M67b / ea0860b M67c / e10f399 M67d / be8b4ce M67e / b8d17aa M67f / abeb851 M67g)
- 日付: 2026-09-05
- 検証機: 開発機 (Release / Debug 両方を `/p:MyeWarnAsError=true` で再ビルド、WARP 撮影 + 実 GPU のエディタ操作)

```
REVIEW: PASS
round: 1
軸 (1-5):
  製品の深度: 4 — 受け入れ条件の外側を 9 通り突いて全部通った: 奇数解像度 333x211 + 可視レイ + anim seed (真っ黒 0 px、平均輝度差 0.15%) / `--rt-restir-visray` を Forward と Deferred(--rt-refl 無し) に渡して素の絵と tol=0 一致 (無害) / `--rt-class-override 7` と `-5` が off と tol=0 一致・`3` は 310850 px 変化 / `--rt-refl --rt-gi --rt-shadow --rt-restir-spatial` 併用で exit 0 / debug 12 が `--rt-refl` `--rt-restir` 無しでも有りと tol=0 一致 (needRefl の強制) / 音響デモ + `--synth-input` + 可視レイ frame 120 で exit 0。残る穴は既知制限として ADR-016 に書かれている (可視レイ off の光漏れ / biased / スキンメッシュ不在)。minor 5 件 (UI の無効判定と折り返し、未使用 CB 項目、古いコメント、JSON 3.0 の扱い) は目的を損なわない
  機能性: 4 — A1 (22 枚 tol=0 含め全 maxDiff=0) / A2・A14 (temporal・spatial とも 2 run tol=0、golden とも tol=0) / A3 (selftest + debug 13 の色 + **Inspector の Combo 往復を実機で確認**) / A4 (selftest 全 PASS、積分 0.8837+0.1166) / A6 (M 平均 G 79.9 → 131.6、M=16 が 20949 px・M=32 が 1596 px、フリッカー 0.206 vs 0.913) / A7 (a) 目標帯 3 矩形とも spatial 0.367/0.343/0.255 > temporal 0.342/0.239/0.181 = 既定 off の根拠を再現、(b) 鏡面 0.198 ≤ 0.206×1.05・輝度 −0.18%、(d) override 0 と 3 が 18230 px 相違、(e) debug 14 でクラス色 / A8・A9 (check_rules 0/0) / A10 (replay_verify PASS) / A11 (restir 2.523 / off 0.000 / visray 5.101 ms) / A12 (文書 + golden) / A13 (**スライダ即時反映と Reset を実機で確認**)。minor のみなので 4
  ビジュアルデザイン: 4 — 実機 (RTX、1600x1200 窓) で debug 12 のヒートマップは Default=黄 / Prop=緑 / 空・粗面=赤に分かれ (g\06_debug12.png)、Default の M cap を 16→2 に動かすと Default 面だけ橙へ落ち Prop の緑は残る (g\08_mcap_low.png)、Reset で戻る (g\09_after_reset.png)。クラス上書き Prop で debug 14 が全面水色 → Off で元の配色 (g\12_d14_prop.png / g\14_d14_off.png)。golden `demo_render_rtrefl_restir.png` は柱・回転十字の映り込みが写っており真っ黒/真っ白ではない。減点はサブメニューの高さ (指摘 2) と、強制 on 中の灰色表示 (指摘 1)
  コード品質: 4 — Debug / Release とも `/p:MyeWarnAsError=true` で警告 0、check_rules 0/0、diff 全読 (60 ファイル、+5780/−139)。「仕様との差分」に無い変更は見つからなかった。HLSL ⇄ C++ のミラー (rt_restir_common.hlsli ⇄ RtMath.h) は式が一致、規則 9 の登録 3 組も実物で確認。減点は M67f 以降に実態と食い違ったコメント 2 か所 (RtPasses.h:60 / :197) と、読み手のいない CB 項目 `gRsFrameIndex`
指摘:
  1. [minor] 宛先: coder — RT Debug 12 / 14 が `view.rtReflRestir` を強制 1 にしている間 (RenderSystem.cpp:1149)、ReSTIR Tuning サブメニューは `BeginDisabled(!ctx.renderSystem->rtReflRestir)` (EditorApp.cpp:1250) で灰色になり、スライダが効く状態なのに触れない — 根拠: g\15_restir_off_menu.png (ReSTIR トグル off + debug 14 で reservoir のクラス色が描かれつつサブメニュー全体が灰色)。同じ理由で台帳の reviewer 手順「ReSTIR を off → on → off とトグルしてデバッグ 12 が赤に戻る」は 12 を表示したままでは起きない (トグルは無視される)。履歴のリセット自体は健全: RT Debug Off (トグル off) を挟んで 12 へ戻すと、クリック 48 ms 後の連写で全面が一様な低 M (橙、Prop の緑が消えている) → 115 ms で飽和 (g\25_regrow_0_48ms.png / g\25_regrow_1_115ms.png、矩形 (540,400)-(640,660) の平均 G 190.9 → 222.1) — 期待: BeginDisabled の判定を実効状態 (`rtReflRestir || rtDebugMode == 12 || rtDebugMode == 14`) にする。台帳の実機手順は「RT Debug Off を挟む」に読み替え (司会)
  2. [minor] 宛先: coder — ReSTIR Tuning サブメニューは項目 19 個 + スライダ 18 本で約 800 px あり、クライアント高 861 px (1400x900 窓) では下端で切れて `Reset to defaults` と GPU 時間の行が見えない — 根拠: g\03_tuning.png (A-Trous の行が最下段で Reset が無い) と g\07_tuning_open.png (1600x1200 では全部見える)。EditorApp.cpp:1246 の `BeginMenu(Restir_Menu)` 直下に 5 クラス × 3 本を縦に並べている構造 — 期待: 900 px 級の窓でも Reset に届くようにする (クラス表を子メニューに畳む / Reset を先頭側へ / 2 列化のいずれか)
  3. [minor] 宛先: coder — `RtRestirCB::frameIndex` を毎フレーム詰めている (RtPasses.cpp:786) が、HLSL 側の `gRsFrameIndex` (rt_restir_cb.hlsli:34) は M67f round 2 でタップ回転のフレーム項を外した後どのシェーダも読んでいない (`grep gRsFrameIndex assets/shaders` = 宣言 1 件のみ)。コメント「タップ回転の乱数列」も嘘になっている — 期待: 項目を落として明示パディングにする (240 B / offsetof 160 は不変) か、少なくとも「予備 (未使用)」に書き換える
  4. [minor] 宛先: coder — M67f (ping-pong・書き戻し無し) 以前のコメントが残っている: RtPasses.h:60「reservoir の組 A の rad と nrm」(実体は `slot.set[slot.write]` = rt_refl が今フレーム書いた面、RtPasses.cpp の `result.reservoirM = slot.set[slot.write].rad.SRV()`) / RtPasses.h:197「B (t11-t15) を読み A (u1-u5) へ書き戻しつつ」(実体は u0 のみ、RenderRestirSpatial は UAV を 1 本しか張らない) — 期待: 2 か所を実態に合わせる (M66l と同型の衛生)
  5. [minor] 宛先: planner — `.mat.json` の `"reflectionClass": 3.0` (値は整数だが JSON 上は float) は GpuResources.cpp:1022 / InspectorWindow.cpp:1463 の `is_number_integer()` が false になり黙って 4 (Default) に落ちる。spec §4.1「非整数 = 4」の「非整数」が JSON の型なのか値なのかが未定義で、AssetOpsSelfTest は `1.5` しか固定していない (読み判定。実走はしていない) — 期待: 仕様で確定する (値が整数なら受ける、が Python 等の書き出しに強い)。受けるなら coder が 2 か所 + selftest に `3.0` の 1 ケース
検証した手段:
  - ビルド: MSBuild Debug / Release とも `/p:MyeWarnAsError=true` → exit 0、警告 0 (実機確認 5)
  - `bin\x64\Debug\Editor.exe --selftest` → exit 0 (`rt: restir` 全 PASS、積分 0.8837+0.1166=1.0003 / 0.9982+0.0019、RIS 0.1004/0.2013/0.6984、temporal loop M=16)
  - `pwsh -File tools\check_rules.ps1` → 0 error / 0 warning
  - `tools\shot_verify.bat` → [PASS] 22 shots、全枚 maxDiff=0 (`=== shot:` 22 件)
  - `tools\replay_verify.bat` → [PASS] 7 シーン + snapshot 往復 + タイムトラベル + 規則検査
  - ヘッドレス probe (Release / WARP、`scratchpad\p\`): a14_1/2 (temporal) tol=0 PASS、a14s_1/2 (spatial) tol=0 PASS、a14_1 vs golden tol=0 PASS、temporal vs spatial 21757 px 相違 / m12_f3 → f40 平均 G 79.87 → 131.60、f40 の色分布 (231,231,0)=20949 px・(0,231,0)=1596 px / fl_on vs fl_off (鏡面 (330,255)-(470,400)) 0.20609 vs 0.91335、輝度 83.377 vs 83.314 / sp_40/41 (spatial on) 0.19839、輝度 83.223 / cls0 vs cls3 18230 px / d13 override 7・−5 は tol=0 一致、3 は 310850 px / fwd・def の `--rt-restir-visray` 単体 = 素の絵と tol=0 / d10 off/on tol=0 / odd 333x211 黒 0 px / all_lanes exit 0 / gpu_on 2.523 ms・gpu_off 0.000・gpu_vis 5.101 (frames 20) / 音響 tb_t vs tb_s (debug 11、player/enemy/床) 0.342/0.239/0.181 vs 0.367/0.343/0.255 / ac_synth on/off maxDiff 2 (7307 px) / ac_vis_120 exit 0
  - 見た画像: tests\golden\demo_render_rtrefl.png / demo_render_rtrefl_restir.png、p\d13_3.png、p\d14_40.png、p\odd_d12.png、p\ac_d12_120.png、p\ac_d12_synth_120.png、p\ac_d14_120.png、p\ac_synth_120.png、g\00〜34 (エディタ実機 33 枚)
  - 実機 (Release Editor.exe `--render-demo --deferred --rt-refl --rt-restir --frames 100000000`、Win32 入力の自動操作): (1) Inspector: demo_bump.mat.json の Combo が Default (欠損 = 4) → Prop → Save → 別フォルダ経由で再選択 → Prop のまま、ファイルに `"reflectionClass": 3` (g\31〜34、検査後に `git checkout` で復元・status clean) / (2) A13: Default の M cap 16 → 2 で debug 12 が即時に橙へ、Reset で 16 と黄へ復帰 (g\07〜10) / (3) off → on → off: 指摘 1 のとおり (g\15、g\25_*) / (4) クラス上書き Off ⇄ Prop で debug 14 が即時に変わる (g\11〜14) / (6) ADR-016 ⇄ engine_spec §6.4 ⇄ README ⇄ CLAUDE.md を読み合わせ: reservoir 56 B/px・2 パス・書き戻し無し・spatial 既定 off・debug 12/13/14 の意味・CLI 5 本・golden 3 枚・kCookVersion 2 が全部一致。ADR の決定 1〜10 は spec §2 の S1/S2/S3/S4/S5/S6/S7/S8/S12/S15 に引ける
  - 読んだ範囲: spec.md 全文 / sub-01〜07 全文 / harness.md / plan-original.md (参照) / 30f4993..HEAD の diff 全ファイル (シェーダ 8 本、RtPasses・RtTypes・RtMath・RtScene・RenderSystem・DeferredPath・EngineLoop・両 main・EditorApp・Inspector・GpuResources・DemoContent・Cook・AssetOps・LocalizationTable・check_rules・shot_verify.bat・ci.yml・ADR-016・engine_spec・README・CLAUDE.md、RtSelfTest は追加テストの一覧と実行ログ)
  - 未確認: J ≠ 1 の temporal 経路 (カメラ / 受け側が動くフレーム) は GPU では観測していない — 音響デモは `--synth-input` でもカメラが動かず (ac_d12_synth_120 は静止と同じ画角)、CPU selftest (A4 の Jacobian 4 件) と静止シーンで M が cap まで伸びる事実 (= rpos 配線) が根拠。W クランプ無しの firefly も同じくカメラ移動下では未観測 (S5 でユーザー)。合成後の音響デモ指標 (spatial 5 条件) は coder の数値を再測していない (反射レーン単体 debug 11 で再現したので判定には十分)
前回指摘の消込: (round 1 のため無し)
```
