# review-1 — m80-destruction

- 対象: `73c8d76..HEAD` (d973c32)。f17ad63 (M76 Deep-Modal、ユーザー本人のコミット) は除外
- 日付: 2026-09-26
- 検証環境: WIP 抜きの worktree `C:\HAL\mye_rv80_r1_1454` (HEAD d973c32 を detached で checkout)。一時プローブはこの worktree にだけ入れ、実行後に revert した (差分の写し: `C:\Users\akita\AppData\Local\Temp\claude\C--HAL-MyEngin\bc8aa3e8-1cc1-44bf-aefb-a18425fbda6f\scratchpad\review_probe.diff`)。本体の作業ツリーには触れていない

```
REVIEW: FAIL
round: 1
軸 (1-5):
  製品の深度: 2 — 主な使い方 (Inspector で焼く → シーンを保存 → 開き直す / Runtime で動かす) で割れない (指摘 1)。同じ名前のエンティティ同士で .mfrac が衝突し、別の物の破片が黙って入る (指摘 2)。既定の strength では普通の衝突で割れない (指摘 5)。ボクセル解像度は 16..256 と案内しているが、80 以上で失敗する (指摘 6)
  機能性: 2 — replay_verify 全 job PASS、Debug/Release の --selftest PASS (digest 一致)、shot_verify は既知の 4 枚を除いて PASS、check_rules PASS。ただし、どのテストもデモもメモリ登録か同じセッションのライブラリしか通っておらず、ファイル資産の経路 (セッションをまたぐ) は実行すると割れない。受け入れ条件 12 の「デモのスクショ」で、壁に穴が開いて見えない (指摘 7)
  ビジュアルデザイン: 3 — 割れる前は元のメッシュと区別できず、割れた後は断面 (innerMaterial) が見え、スキンの腕も上半分だけが落ちる (frac_8/35/60/590.png、golden 2 枚)。ただし固定の壁は、スクリプトで割っても球を当てても、全 tick で無傷に見える (指摘 7)。縮んで消えるでは、スケール付きのルートで大きさが飛ぶ (指摘 3)
  コード品質: 3 — 層の分け方 (焼き / 資産 / 構築 / 破断) と存在ゲートはきれいで、テストも厚い。一方で、キャッシュのキーが誤っている (ライブラリはパス、FractureSystem はエンティティ、Commit は GUID でない値を保存) ことが原因の不具合が 3 系統ある (指摘 1・2・9)。スケールの上書き (指摘 3) もある
指摘:
  1. [blocker] 宛先: coder — Inspector で焼いた .mfrac は、次のセッション (エディタの開き直し / Runtime.exe) で解決されない。そのため、Destructible は一生割れない — 根拠: CommitFractureBake が `fractureAsset = HashStr("guid://<16hex>")` を保存する (src/Editor/FractureBakeCommit.cpp:71)。ResolveFractureAsset は、この値を GUID として `assetguid::ResolvePath` に渡している (src/Engine/Engine/FractureSystem.cpp:163。ResolvePath は GUID→パスの表引き、src/Engine/Engine/AssetDatabase.cpp:315-318)。起動時の資産走査は .mfrac を読まない (src/Engine/Engine/DemoContent.cpp:3157- の RegisterAssetLibraries に分岐が無い)。一時プローブ P12 (本物の AssetDatabase + CommitFractureBake → SaveToJson → 新しい AssetDatabase/FractureLibrary で LoadFromJson) の結果: `fractureAsset=324dc5db39bc697c`、`.meta` の GUID は `a3a52e81694e6725`、`ResolvePath(fractureAsset)=''`。同じセッションでは breaks=1。新しいセッションでは breaks=0 で、`[ERROR] [fracture] Crate: fracture asset is not loaded, this destructible will not break` が出る。対照として、手で LoadFromFile してから回すと breaks=1。同じ原因で、開き直した直後の Inspector は FindByAssetId が null を返し、正常な Destructible に赤字「資産と子が一致していません。実行時に割れません」を出す (InspectorWindow.cpp DrawDestructibleNotes の FindByAssetId)。仮に解決できても、.mfrac を読むのは最初の tick の物理の後なので、1 tick 目は凸包が未登録のまま (ConvexColliderLibrary::Get が null → shape=5 を無視) になる。--fracture-demo も SelfTest も、メモリ登録か同じセッションのライブラリしか通らないため、この経路は一度も実行されていない — 期待: AssetRef の値を資産の GUID (他の AssetRef と同じ規約) にそろえ、解決できるようにする。シーンの読み込み時 (最初の物理 tick より前) に、参照している .mfrac を読んで登録する。「焼く → 保存 → 新しい AssetDatabase/ライブラリで読み直す → 割れる」「開き直した直後に Inspector が生成済みと表示する」を SelfTest に足す (planner は受け入れ条件へ追加を)
  2. [blocker] 宛先: planner — 保存先の名前が衝突し、FractureLibrary は上書きされたファイルを読み直さない。この 2 つが重なって、別の物の破片が黙って組まれ、資産が壊れる — 根拠: spec §4.3 の保存先は「`<エンティティ名>_<seed>_<pieceCount>.mfrac`、既存なら確認なしで上書き」。FractureLibrary::LoadFromFile は、読み込み済みのパスなら古いハンドルを返す (src/Engine/Engine/Physics/FractureLibrary.cpp:80-86)。プローブ P2c: 別々のエンティティ 2 つを既定風の名前「Cube」にし、seed=1・pieceCount=16 のまま、立方体と細長い板をそれぞれ Commit した。結果は `.mfrac files=1 sameAsset=1`。板の焼き結果は origin0.x=2.586 なのに、板のハンドルは origin0.x=0.320 (= 立方体の焼き結果) で、板に立方体の破片が組まれた。ディスク上は後から焼いた内容で上書きされるので、再起動後は立方体側の子 (立方体の破片の姿勢) に板のメッシュが載る。破片数が同じなので ERROR も出ない。P2/P2b: 同じエンティティのメッシュ・openMeshMode・voxelResolution を変えて焼き直すと、ファイルは新しい内容 (srcHash=2002) になるが、ライブラリと組まれた子は古い内容 (srcHash=1001、origin0 が 1 回目の焼き結果と一致) のままになる — 期待: planner が保存名の一意性を決める (fileId や GUID を名前に含める / 衝突したら連番 / 上書き前に確認 など)。その上で coder が、LoadFromFile / Commit で上書き後の内容を必ず登録し直す。同名 2 体と焼き直しの回帰テストを受け入れ条件に足す
  3. [major] 宛先: coder — afterBreak=3 (縮んで消える) が、リーダーの scale を絶対値 {s,s,s} で上書きしている。スケール付きのルートでは、縮み始めに大きさが飛び、非一様スケールも失われる — 根拠: src/Engine/Engine/FractureSystem.cpp:611-619。プローブ P5 (ルートの scale=2、afterBreakTicks=2、fadeTicks=4) で、リーダーの scale は tick0-1 が 2.0、tick2 が 1.0 (いきなり半分)、以後 0.75 → 0.5 → 0.25 と推移した。SelfTest (17d) は scale=1 のルートしか見ていない — 期待: 縮み始めの scale に係数を掛ける (開始時の値はハッシュ対象の欄に持つか、ルートのワールドスケールから決定的に導く)。スケール 2・非一様スケールのテストを足す
  4. [major] 宛先: planner — スキンの破片は「原点 = 骨の原点」なので、ApplyFractureDamage の半径判定と onBreak の point が空間的に意味を持たない — 根拠: 骨空間で焼くときに `piece.origin = {0,0,0}` にしている (src/Engine/Engine/Physics/FractureSkinBake.cpp の AssignFractureBonesAndTransform)。BuildFracturePieces はその原点を LocalTransform に入れ、PartFollow で骨の位置に置かれる。ApplyFractureDamage は破片エンティティの姿勢で距離を測る (src/Engine/Engine/FractureSystem.cpp:878-884)。そのため、同じ骨に割り当てられた破片は全部同じ位置になる。radius>0 だと骨ごと全部に当たるか、全部外れる。radius<=0 だとエンティティ index の小さい 1 つに当たり、手首を撃っても肩の破片が壊れる。onBreak の point も骨の原点になる。spec §4.1「スクリプト」は「原点」で定義しているが、スキンの破片の原点が骨になることが考慮されていない。要件はスキンメッシュを含み、スクリプトからの損傷も含む — 期待: 損傷と onBreak の位置を「破片の体積重心 (ワールド)」と定義し直す (スキンなら骨空間の重心を資産に持つ / 凸包の重心を使う)。スキンで局所的に当てるテストを足す
  5. [major] 宛先: planner — 既定の strength=5000 [N] では、普通の衝突で割れない。「ボタンを押すだけで壊せる物」(spec §1) が既定値では成り立たない — 根拠: プローブ P6 (8 破片の箱、床あり)。strength=5000 で 8kg の箱に 20kg・30m/s の球を当てても割れない (破片荷重の最大 10549N)。20kg・10m/s (3521N) でも、1kg の箱に 5kg・10m/s (517N) でも、既定質量 1kg の箱を 5m から落としても (551N) 割れない。50kg を 5m から落としてようやく 1 片、strength=200 なら同じ衝突で 4 塊に分かれる。--fracture-demo・SelfTest・ベンチはすべて strength を 100〜200 に下げていて、デモのコメント自身が「既定値 (5000N) ではこのデモの衝突では割れない」と書いている (src/Engine/Engine/DemoContent.cpp の BuildFractureShowcaseScene) — 期待: 既定値の根拠を実測で決める (例: 既定質量・典型速度で割れる値、または質量に比例するしきい値)。ツールチップに目安を書く。「既定値のまま、決まった衝突で割れる」を受け入れ条件に足す
  6. [major] 宛先: planner — voxelResolution の範囲 16..256 (Inspector で選べる) が実装で成り立たない。80 以上では最も単純な開いた箱でも失敗する。焼きは取り消せず、エディタ終了時は焼き終わるまで待たされる — 根拠: プローブ P10 (Release、開いた箱、16 破片) の結果は次のとおり。res64 は成功で 17.9s、res72 は成功で 22.3s。res80 は 26.4s 後に失敗 (「破片 (シード 11) の面が欠けている」ratio=0.0306)。res96 は 36.7s 後に失敗 (ratio=0.0041)。res128 は 68.5s 後に失敗。sub-14 と bench は 64 までしか測っていない。FractureBakeService::Shutdown はワーカーを join する (src/Editor/FractureBakeService.cpp の Shutdown) ので、res256 を押したまま閉じると終了が止まる。取り消しは無い (AGENTS §3.4「理由の分からない無応答を作らない / キャンセル」) — 期待: 上限を実測で成功する値 (例 64) に下げる、または 256 まで直す、のどちらかを決める。長い焼きの取り消し (または終了時に打ち切る) を足すかを決める
  7. [major] 宛先: planner — 受け入れ条件 12 の「デモのスクショ」で、固定の壁が「叩いた所だけ抜け」て見えない。Q-14 (打ち抜きなし) の見た目への影響が大きいことを示す証拠でもある — 根拠: 自分で撮った `scratchpad\frac_8.png` (tick 5 にスクリプトで割った直後)、`frac_35.png` (着弾)、`frac_60.png`、`frac_590.png`、golden の `tests\golden\fracture_after.png`。どれでも中央の灰色の壁は無傷に見える。プローブ P8: スクリプトの ApplyFractureDamage(root 中心, radius 0) で外れたのは、中心から 0.14m の内部寄りの破片 index 10。90 tick 後もワールド位置 (2.908, 0.578, -0.072) から 1mm も動いていない (周りに囲まれ、初速 0 のため)。球が当たって外れた破片も、初速 0 で床に支えられて元の位置に残る。ユーザーが最初に試しそうな「壁を撃つ」で、何も起きないように見える — 期待: Q-14 をこの画像を添えてユーザーに上げる。デモは表面の破片が外れて落ちる配置にし、受け入れ条件 12 の絵を満たす (例: 薄く高い壁 / 床から浮いた部分を撃つ)
  8. [minor] 宛先: coder — Play 中に一度割れると、Inspector が赤字「資産と子が一致していません。実行時に割れません」を出す — 根拠: DrawDestructibleNotes は直下の子の数 (CountFracturePieceChildren) を資産の破片数と比べている (src/Editor/Windows/InspectorWindow.cpp の DrawDestructibleNotes)。分かれた破片はルートの親の下へ移る。プローブ P7: 1 片が外れた後、子 3 / 資産 4 — 期待: broken を考慮した判定 (FractureSystem の PiecesMatchAssetNow と同じ規則) にする
  9. [minor] 宛先: coder — FractureSystem::assetCache_ のキーが fractureAsset ではなくルートのエンティティになっている。「資産参照から一意に決まる値なので安全」というコメントと食い違う — 根拠: src/Engine/Engine/FractureSystem.cpp:759-781。プローブ P1: 生きているルートの fractureAsset を 4 破片の資産から 8 破片の資産へ差し替えると、同じ FractureSystem では割れず、`piece index is out of range` の ERROR が出る。新しい FractureSystem なら割れる。エディタの Play はシーンを読み直して世代が変わるので、普段は当たらない。Play 中に欄を書き換えたとき (Inspector / SetComponentField) に当たる — 期待: キーを fractureAsset.value にするか、ハンドルとキーの対応を毎回確かめる
  10. [minor] 宛先: coder — スキンの Destructible を選んでいる間、Inspector が毎フレーム、.mmdl のクックキャッシュ全体を読み込み、検証し、デシリアライズしている — 根拠: DrawDestructibleNotes → ModelCook::TryLoadCookedMeshVertices (src/Engine/Engine/Asset/ModelCook.cpp:355-376) → CookedCache::ReadValidated が毎回ファイル全体を読む (src/Engine/Engine/Asset/CookedCache.cpp:149-)。計測はしていない (コード上の事実) — 期待: ボタンを押したときだけ読む、またはメッシュ単位でキャッシュする
  11. [minor] 宛先: coder — 割れた tick では、ApplyStructuralChanges まで「LocalTransform は新しい親基準、階層は古い親」のままになり、ワールド姿勢を LocalTransform から組み立てると二重にずれる — 根拠: ReparentKeepWorld は LocalTransform を即座に書くが、SetParent は tick 末まで遅れる (src/Engine/Engine/FractureSystem.cpp:78-107)。プローブ P8: 割れた直後に ComposeEntityWorldPose で読むと (5.908, 1.078, -0.072)、正しくは (2.908, 0.578, -0.072) (壁の位置 (3, 0.5, 0) 分だけずれる)。onBreak や LateUpdate から ApplyFractureDamage を呼ぶと、その tick だけ外れたばかりの破片の距離が誤る — 期待: この窓の制約を文書化するか、姿勢の書き換えを構造変更の適用と同時にする
  12. [minor] 宛先: coder — `.mfrac` の読み込みが、インデックスの範囲 (outer/capIndices < 頂点数) と隣接先 index を検査していない。一部のバイトだけ壊れたファイルは通り、範囲外インデックスのメッシュが登録される — 根拠: src/Engine/Engine/Asset/FractureAsset.cpp:117-131 (件数の上限と余りだけを見ている)。spec §4.2「境界検査付きで、壊れたファイルでも落ちない」— 期待: インデックスと隣接先を検査し、失敗させる。1 バイトだけ壊したファイルのテストを足す
  13. [minor] 宛先: coder — 隣接が 32 を超えたときの切り捨てが破片ごとに片側で行われる。i<j で i 側だけが j を落とすと、その接着は二度と切れない — 根拠: 切り捨ては破片ごとに独立 (src/Engine/Engine/Physics/FractureBake.cpp:1240-1265)。破断は i<j の組を i 側の表からしか見ない (FractureSystem.cpp:284) が、連結成分は j 側の表でも union する (FractureSystem.cpp:377-389)。sub-02 の受け入れ条件「対称 (i→j があれば j→i)」にも反する。プローブ P4 (256 破片の箱×2・板・棒) では最大隣接 25 で、切り捨て 0・非対称 0。今の形状では起きていない、潜在的な問題 — 期待: 両側で同じ組を落とす (対の面積で決める)。対称性を切り捨て後にも確かめるテストを足す
検証した手段:
  - `tools\replay_verify.bat` (worktree で Debug/Release をビルドし直してから) → 14 job 全 PASS (fracture 含む、`[PASS] replay consistency ... 9 scenes`)
  - `bin\x64\{Debug,Release}\Editor.exe --selftest` を同時に実行 → 両方 exit 0。fracture digest (lshape 0x521C987A83D781F7 / torus 0xD9B0EEB57B686B71) は Debug と Release で一致
  - `tools\shot_verify.bat` → FAIL 5 枚: parts / joints / acoustic_forward / acoustic_deferred (既知の 4 枚、除外) と flow_title。flow_title は、worktree で C# (MyeScripting.dll) を作る前に flow シーンを生成したため FlowMenu が付かなかった、という環境由来。`tools\build_managed.bat Release` の後にシーンを作り直して撮ると maxDiff=0 で PASS。fracture_before は maxDiff 0、fracture_after は maxDiff 1 で PASS
  - check_rules (replay_verify の rules job) → 0 error / 0 warning
  - `Editor.exe --fracture-bench` (Release) → bench.md と同程度 (焼き 16/32/64/128/256 = 21/49/132/345/1043 ms、voxel res32/48/64 = 4.6/10.6/19.7 s)
  - `Runtime.exe --fracture-demo` を frame 8 / 35 / 60 / 200 / 590 で撮影して目視 (`scratchpad\frac_*.png`)。golden 2 枚も目視
  - 一時プローブ (worktree の FractureEditorSelfTest.cpp / EditorMain.cpp に `--m80probe` / `--m80p2c` / `--m80p12` を追加、Release で実行、実行後に revert): P1 キャッシュ、P2/P2b/P2c 資産の上書きと衝突、P3 新しいライブラリでの FindByAssetId、P4 隣接の対称性、P5 縮みのスケール、P6 既定 strength、P7 割れた後の子の数、P8 壁の外れ方、P10 ボクセル解像度、P12 セッションをまたぐ解決。ログは `scratchpad\probe*.log` / `.err`
  - diff 全読 (M80 の 15 コミット): FractureSystem / FractureBuilder / FractureLibrary / FractureAsset / FractureBakeCommit / FractureBakeService / InspectorWindow の差分 / PhysicsSystem の差分 / RenderSystem / TickRunner / EngineLoop / ABI (EngineAPI.h / ScriptAPI.h / ScriptTypes.h / Interop.cs / MyeScript.cs / ScriptRuntime.cs / Bootstrap.cs / EngineApiTable.cpp / ManagedHost / ScriptHost) / Components / LocalizationTable / DemoContent / FractureSkinBake / replay_verify.bat / shot_verify.bat / engine_spec §10.8 / ADR-021。FractureBake / FractureMesh / FractureVoxel は決定論の観点 (並び替えのタイブレーク、ハッシュコンテナの走査順、乱数) に絞って読んだ
  - 「仕様との差分」に無い変更は見つからなかった (RtScene のログ、EditorApp の inPlayMode、各 SelfTest の版番号の更新は、いずれもサブの SELF_EVAL に記載がある)
前回指摘の消込: (round 1 のため無し)
```
