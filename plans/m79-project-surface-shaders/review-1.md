# review-1 — m79-project-surface-shaders

- 対象コミット範囲: 3b55f4a..a08d8d4 (M79a e492a85 / M79b 2ed28d9 / M79c 4f7cbad / M79d 99654de / M79e a08d8d4)
- 日付: 2026-09-24
- 一時検証物: `C:\Users\akita\AppData\Local\Temp\claude\C--HAL-MyEngin\8864d946-863a-46b5-9312-6ffea53e1a0b\scratchpad\` の `rv1\` (合成シーン) と `rvw\` (Water プロジェクトのコピー。canonicalRoot は削除済み。本物の Water には書き込んでいない)

```
REVIEW: FAIL
round: 1
軸 (1-5):
  製品の深度: 2 — Water main のコピーで作者の式 (浮世絵リム・破波の頂点アニメ・WaterWave の surfaceMaterial) が Forward / Deferred / TAA で動き、TAA 差分も縁の AA だけでゴーストは出ない (rvw\taa2_diff.png)。ただし「サーフェスと普通のメッシュを混ぜたシーン」で既存物体の影や instanced ジオメトリが消える (#1 #2)。頂点変位で AABB の外へ出た形はカリングで丸ごと消える (#3)。大波の用途ではどちらも踏む
  機能性: 2 — 受け入れ条件 1〜10 は SelfTest (Debug/Release)・Runtime.exe 実経路・失敗時マゼンタ・Properties の WARN で確認できた。条件 11 (サーフェスを使わないシーンは不変) は既定シーンでは base と HEAD がビット一致した。しかし、サーフェスが 1 個でも混ざると、既存の forward_lit 物体の CSM 影が消え、instanced ジオメトリも消える (#1 #2)。これは回帰
  ビジュアルデザイン: 3 — マゼンタ (rv1\fail_def.png)、サーフェスの受影 (rv1\recv_def.png)、velocity (rvw\vel.png)、浮世絵リム (rvw\taa2.png) はどれも意図どおり。一方で影の欠落 (rv1\shadowA.png) とジオメトリの消失 (rv1\vtexA.png) は画面で一目で分かる。エンジン同梱の WaterGerstner サンプルは Water main では真っ黒になる (#6)
  コード品質: 3 — 名前解決バインド・生成エントリ・キャッシュ・kFieldNoHash の構造は筋が良く、コメントも「なぜ」を書いている。ただし名前で張ったスロットを固定スロット前提の後続描画へ戻す処理が、ShadowPass には無く、ForwardPath では不完全 (#1 #2)。混在順序の回帰テストも無い。PerMaterial CB のサイズを全 cbuffer から取っている (#4)
指摘:
  1. [blocker] 宛先: coder — ShadowPass で、サーフェスの影エントリが名前解決で張った CB / SRV が戻されない。そのため同じカスケード内で後に描く非サーフェスのアイテム (forward_lit、失敗時フォールバックの shadow_depth、instanced run) が壊れた b0 / t0 を読み、影を落とさなくなる — 根拠: src/Engine/Renderer/ShadowPass.cpp:215 と :218 が objectCB_ (b0) と instance SRV (t0) を**ループ前に 1 回だけ**張る。サーフェスのブロック (:254-:291) は BindSurfaceNamedCB/SRV で任意スロットを上書きして continue し、戻さない。再現: rv1\assets\scenes\shadowA.scene.json (Floor, Surf=Static.surface, LitMid, LitRight の順) → rv1\shadowA.png で forward_lit 2 個の影が消える。エンティティ順だけを入れ替えた shadowB.scene.json → rv1\shadowB.png では 3 個とも影が出る。Deferred も同じ (rv1\shadowA_def.png)。失敗マテリアル 2 個の場合も、後に描く方だけ影が消える (rv1\fail.png / fail2.png)。VS でテクスチャを読むサーフェスの後の instanced 影も消える (rv1\vtexA_def.png) — 期待: サーフェスのアイテムを描いた後は、深度 VS/IL・objectCB_ (b0)・instance SRV (t0) を戻す (ForwardPath の restoreForwardLitBindings 相当)。「サーフェス → forward_lit / instanced / フォールバック」の順で影深度を read-back する SelfTest を足す
  2. [blocker] 宛先: coder — ForwardPath の restoreForwardLitBindings が VS の SRV t0 (instancing のワールド行列バッファ) を戻さない。VSMain で Texture2D を読むサーフェス (高さ・ノイズでの波の変位はこの用途の定番) を 1 個描くと、それ以降の instanced forward_lit が全部消える — 根拠: src/Engine/Renderer/ForwardPath.cpp:404 は instance SRV を DrawItems の前に 1 回だけ張る。:476 の restore ラムダは VS CB 0-1 / PS CB / PS SRV 1-9 / サンプラしか戻さない。再現: rv1\assets\shaders\VTex.surface.hlsl (VS で `_HeightTex.SampleLevel`) + 同じメッシュ・同じマテリアルの forward_lit キューブ 7 個 → rv1\vtexA.png では床もキューブも消える。rv1\vtexA.err には `DrawIndexedInstanced: The Shader Resource View dimension declared in the shader code (BUFFER) does not match the view type bound to slot 0 of the Vertex Shader unit (TEXTURE2D)` が出る。VS テクスチャ無しのサーフェス (shadowA) では起きない — 期待: restore で VS t0 に instance SRV を戻す (無ければ null)。名前で張りうる他の VS スロットについても、後続の固定スロット前提を洗い出す。「VS テクスチャ付きサーフェス → instanced run」の SelfTest を足す
  3. [major] 宛先: planner — 視錐台カリングが変位前のメッシュ AABB で判定する。頂点変位で AABB の外へ出た形 (大波の巻き込み・うねりの山) は、元の形が画面外になった瞬間に丸ごと消え、影も落とさない (影のキャスターはカメラでカリング済みのキューから取る)。仕様 (§3 / §4) にこの扱いが無い — 根拠: src/Engine/Engine/RenderSystem.cpp:1107 `RenderableInFrustum(frustum, c.world, aabbMin, aabbMax, ...)`。再現: rv1\assets\shaders\Lift.surface.hlsl (VS で world.y += 8) の板を y=-3 に置き、カメラを見上げにする (liftA) → rv1\liftA.png では空だけが写る。同じ板を変位なしで y=5 に置いた liftB → rv1\liftB.png では写る。liftA と同じ変位でカメラを引いた liftC → rv1\liftC.png では持ち上がった板が写る (シェーダは正しい) — 期待: 仕様で方式を決める。案: マテリアル単位の bounds 余白 (UE の Bounds Scale / Unity の手動 bounds 相当)、またはサーフェスのアイテムはカリングしない。受け入れ条件に「変位でAABBを出た形が消えない」ことを足す
  4. [minor] 宛先: coder — MyEnginePerMaterial のパックで、cbuffer 名を絞らずに全 cbuffer の変数から reflectionVars と cbSize を作っている。GPU の PerMaterial CB が最大の cbuffer (MyEnginePerFrame、数 KB) の大きさで確保される。Properties 名が予約 cbuffer の変数名と一致すると、別 cbuffer のオフセットで書かれる — 根拠: src/Engine/Renderer/GpuResources.cpp:1350-1357 (collect ラムダが refl.vars を全件走査し `cbSize = max(var.cbufSize)`)。SurfaceReflectedVar には cbuffer 名が無い (SurfaceProgram.h) — 期待: MyEnginePerMaterial に属する変数だけを集め、その cbuffer のサイズで確保する
  5. [minor] 宛先: coder — マテリアル Inspector の Properties スキーマキャッシュ matSchemaCache_ が無効化されない。HLSL の Properties を足すとホットリロードで描画側には効くが、Inspector はエディタを再起動するまで古い欄のまま (fxstack の schemaCache も M78 から同じ) — 根拠: src/Editor/Windows/InspectorWindow.h:136 の宣言と、InspectorWindow.cpp:1962 / :2208 の GetOrFetchPropertySchema 以外に、clear / erase の箇所が無い (grep で確認)。実行での確認はしていない — 期待: シェーダの世代 (SurfaceProgram::generation) かファイル更新時刻で、キャッシュを捨てる
  6. [minor] 宛先: coder — 同梱サンプル WaterGerstner.surface.hlsl が gAmbient を使わず、`lit = waterColor * (0.35 + 0.65*ndotl)` だけで色を出す。Water main (太陽の N·L≒0、castShadow なし) では水面が真っ黒になる。お手本としては「環境光の足し方」が欠けている — 根拠: assets/shaders/WaterGerstner.surface.hlsl:80。rvw\taa2.png では水面が黒い。デバッグ用シェーダ rvw\assets\shaders\WaterDbg.surface.hlsl の出力 rvw\dbg_def.png では gSunDirection・gAmbient は届いており、ndotl だけが 0 (エンジン側は正常) — 期待: gAmbient (か空色) の項を足す
  7. [minor] 宛先: planner — 動機の資産 Water/assets/shaders/breaking_wave.hlsl:130-134 は裏面を描く前提 (「裏面側の暗い藍」)。一方、サーフェスは CULL_BACK 固定で、Cull Off は「後回し」になっている。破波の巻き込みをサーフェスへ移植すると内側が抜ける可能性がある — 根拠: 上記の行と src/Engine/Renderer/ForwardPath.cpp:143 / DeferredPath.cpp:347 (CullMode = D3D11_CULL_BACK)。メッシュが両面ジオメトリかどうかは未確認 — 期待: 後回しのままにするなら §7 のリスクに書く。上げるなら両面指定の最小案を検討する
  8. [minor] 宛先: planner — 受け入れ条件 11 / §4.4 の「golden 系が無変更で通る」は、M79 より前から満たせない状態にある。tools\shot_verify.bat は 23 枚 FAIL する (golden は空が黒く、実際の画像はグラデーション空 + IBL)。ただし base 3b55f4a と HEAD の既定シーン (Forward / Deferred、--no-fxaa) はビット一致したので、M79 の回帰ではない — 根拠: 作業用スクラッチの shot.log、tests\actual\demo_forward.png と tests\golden\demo_forward.png の比較。base の worktree ビルドで撮った base_demo_forward3.png / base_demo_deferred.png と、HEAD で撮った head_demo_*.png は getbbox() == None — 期待: golden の撮り直しを別件として台帳の申し送りに積む (M79 の中では直さない)
検証した手段:
  - ビルド: MSBuild MyEngine.sln Debug|x64 (警告 0)。tools\replay_verify.bat が Debug/Release をビルドし、13 ジョブ全部 PASS (8 シーン + snapshot + time travel + rule check)
  - bin\x64\Debug\Editor.exe --selftest と bin\x64\Release\Editor.exe --selftest はともに exit 0。FAIL 行 0 (M79 の SurfaceShader / SurfaceMaterial / SurfaceDeferred / WaterSurface / WaterWave #7 / AssetOps テンプレートを含む)
  - tools\check_rules.ps1 → 0 error / 0 warning
  - tools\shot_verify.bat → 23 枚 FAIL。原因は M79 より前の golden の陳腐化 (#8)。worktree に 3b55f4a を detached で置いてフルの Release をビルドし、既定シーンが HEAD とビット一致することを確認した。worktree は確認後に git worktree remove で削除済み
  - 受け入れ条件の外側 (rv1、Runtime.exe --warp、Forward と --deferred): 変位が AABB の外へ出る場合 (liftA/B/C)、失敗 3 種 (未対応 semantic COLOR / Properties の型大きさ不一致 / ファイル無し) → fail_*.png とログ (マゼンタ・「サーフェス規約」付きエラー・`_Tint` の WARN)、描画順と影 (shadowA/B、fail2)、VS テクスチャ + instancing (vtexA/B)、サーフェス面の受影 (recv_def.png)
  - 実用途 (rvw = Water のコピー): ukiyoe_flat を UkiyoFlat.surface に、breaking_wave を BreakingWaveAnim.surface (gTime で頂点を揺らす) に移植した。WaterWave.surfaceMaterial には WaterGerstner.surface を設定した。fwd / def / vel / taa2 (CameraPostFx.taaOn=true のシーンコピー。scene の taaOn=false が --taa より優先されることも確認) を撮影し、TAA あり / なしの差分 taa2_diff.png はエッジの AA だけ
  - 読んだ範囲: spec.md、sub-01〜05 (実装メモを含む)、harness.md、3b55f4a..HEAD の diff の全体 (Renderer / Engine / Editor / シェーダ。SelfTest は抜粋)
  - 台帳の reviewer 向け 3 点: (a) sub-03 の空時の早期 return は、既定シーンの Deferred で base と HEAD がビット一致したので実地では担保されている。自動テストは依然無い (b) Create メニューの実クリックは reviewer も未確認。コード上は Post / Compute と同じ形の配線 (AssetBrowserWindow.cpp の kCreateSurfaceShader) (c) サーフェス面が CSM の影を受けることは recv_def.png で確認した。サーフェスのキャスターが影を落とすことは shadowA.png の左のキューブで確認した
  - 未検証: 実 GPU (すべて WARP)、Editor のマテリアル Inspector の GUI 操作 (sub-04 の coder のスクショに依存)、シャドウアトラス、スキン + サーフェス
前回指摘の消込: (round 1 のため無し)
```
