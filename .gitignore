bin/
obj/
cache/
.vs/
*.user
*.ilk
*.log
imgui.ini
# スキーマ codegen の生成物 (起動時に毎回再生成される。M50d)
assets/scripts/Generated/
# flow 統合デモの生成シーン (M51j)。版管理された正解はコード側 (DemoContent) —
# replay_verify が毎回組み直す。parts_showcase (cache/) と同じ流儀
assets/scenes/flow_title.scene.json*
assets/scenes/flow_game.scene.json*

# スクショ回帰の撮り直し結果 (M52c)。golden だけを版管理する
tests/actual/

# クラッシュバンドル (M52f)。レガシー起動なら bin/ 配下だが、プロジェクト起動では
# <projectRoot>\crash\ に出る = リポジトリ直下にも落ちうる
crash/

# Rust (MyeCollab, M66a) のビルド成果物。DLL / CLI は bin\ へコピーされる方が正
# (bin\ 自体も gitignore。cargo の target は数百 MB になるので絶対に載せない)
tools/collab/target/
