//====================================================================================
//                          XpbdSelfTest.h
//  MyEngine/ 秋田蓮音                                                      08/27/2026
//                                          XpbdBackend（3 点セット契約）の自己テスト
//====================================================================================
#pragma once

namespace mye {

// XPBD 変形体バックエンド (M60'b) の自己テスト。D3D もウィンドウも作らない。
//
// M60'b は器だけ (ソルバ無し) なので、検査対象は「ECS 外 sim 状態の 3 点セット契約」:
//   1. ハッシュの内容ゲート — 空の池を配線しても既存ハッシュが 1 ビットも動かない
//   2. 池の全フィールドがハッシュ被覆に入っている (変異で必ず割れる)
//   3. SimSnapshot v4 の往復がビット同一 + refs に池が無い構成では節を読み捨てる
// ソルバの数値検査は M60'c の担当。
bool RunXpbdSelfTest();

} // namespace mye
