#pragma once

namespace mye {

// ネットセッション (UDP + 遅延ロックステップ) の回帰テスト (M52h)。
// ★1 プロセス内で host / join の 2 セッションをループバックで実際に繋いで回す —
//   パケット構造体の往復だけを見るテストにすると、ハンドシェイクや冗長送信という
//   「実際に壊れる場所」が丸ごと素通りする。待受ポートは 0 (任意) で開くので、
//   CI ランナーでもポート衝突しない
bool RunNetSelfTest();

} // namespace mye
